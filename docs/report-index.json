[
  {
    "date": "2026-09-26",
    "scan_time": "2026-09-26 12:21",
    "file": "report-2026-09-26.md",
    "platforms": [
      {
        "name": "Amazon Bedrock AgentCore",
        "status": "changed",
        "details": [
          "**`harness-tools` audited for the first time.** Documents five attachable tool types (`remote_mcp`, `agentcore_gateway`, `agentcore_browser`, `agentcore_code_interpreter`, `inline_function`) plus two always-on builtins (`shell`, `file_operations`), the `allowedTools` glob-pattern restriction language, and the credential model for each. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-tools.html",
          "**`harness-command-shell` audited for the first time.** The `InvokeAgentRuntimeCommandShell` WebSocket API behind the already-reported \"Harness: Interactive shells\" release note \u2014 binary frame protocol, dual auth paths, detach/reconnect semantics, and the logging model. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-command-shell.html",
          "**Web Search Tool reachable from a harness.** Not a harness tool type \u2014 it is an AgentCore Gateway target with `connectorId: \"web-search\"`, surfaced to the agent as an MCP `WebSearch` tool. Requires `bedrock-agentcore:InvokeWebSearch` on the Gateway service role and `bedrock-agentcore:InvokeGateway` on the harness execution role. `us-east-1` only. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-tools.html"
        ],
        "tier": "primary"
      },
      {
        "name": "Claude Enterprise (Anthropic)",
        "status": "changed",
        "details": [
          "**Compliance API local session endpoints are out of beta for Claude for Microsoft 365** \u2014 Excel, PowerPoint, Word and Outlook sessions (`product_surface` values beginning with `office_agents`). Source: https://platform.claude.com/docs/en/manage-claude/compliance-sessions#retrieve-local-sessions",
          "**Compliance Activity Feed no longer returns file names, project document names or artifact titles.** The `filename` and `title` fields are now always empty or omitted \u2014 retroactively, including on activities recorded before the change. Names must now be resolved by ID through a separate scope. Source: https://platform.claude.com/docs/en/manage-claude/compliance-activity-feed#understand-the-activity-object",
          "Also on 2026-09-24: refusal billing resumed for `bio`, `frontier_llm` and `reasoning_extraction` pre-output refusals. Billing change, not a capability \u2014 not raised as a finding."
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "AgentCore `allowedTools` does not restrict `InvokeAgentRuntimeCommand` \u2014 the tool allowlist and the shell API are unrelated controls",
        "description": "The `allowedTools` parameter on an AgentCore harness accepts glob patterns (`\"@builtin/shell\"`, `\"@git/read_*\"`, `\"@*-mcp/status\"`) and reads exactly like a sandbox. AWS states verbatim that it **\"scopes LLM tool selection during `InvokeHarness` only. It does not affect `InvokeAgentRuntimeCommand`, which is a separate API with its own IAM action (`bedrock-agentcore:InvokeAgentRuntimeCommand`) that executes commands directly without passing through the LLM.\"** The documented remediation is **\"do not grant `bedrock-agentcore:InvokeAgentRuntimeCommand` in your IAM policies.\"** Per the harness-environment audit on 2026-09-25, that API runs as **root (uid 0)** in the microVM, above the container's `USER` directive. So a harness configured with `allowedTools: [\"@builtin/file_operations\"]` \u2014 no shell, by every reading of its configuration \u2014 is fully shell-capable to any principal holding that one IAM action. Compounding it: **omitting `allowedTools` allows all tools**, a fail-open default. AISPM must treat `allowedTools` and the `InvokeAgentRuntimeCommand`/`InvokeAgentRuntimeCommandShell` IAM grants as **two independent controls on one harness**, and raise a toxic combination when a harness carries a restrictive `allowedTools` list *and* any principal holds either shell IAM action on it \u2014 that pairing is the signature of an operator who believes they have sandboxed an agent and has not",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-tools.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "AgentCore harness tool attachment \u2014 five tool types, arbitrary remote MCP by URL, and plaintext credentials in harness config",
        "description": "`harness-tools` documents the full attachment surface for an AgentCore harness, each a distinct inventory entity: **`remote_mcp`** (any remote MCP endpoint by URL \u2014 AWS notes \"No Gateway required for simple cases,\" so this path carries **no Gateway policy enforcement, no inbound/outbound auth and no access control**), **`agentcore_gateway`** (governed, policy-backed, by ARN \u2014 with outbound auth selectable as AWS IAM, **None**, or OAuth), **`agentcore_browser`**, **`agentcore_code_interpreter`**, and **`inline_function`**. Two builtins \u2014 **`shell`** (bash) and **`file_operations`** (view/create/edit) \u2014 are present in **every session** unless restricted. Critically, a `remote_mcp` tool's `headers` map holds **raw plaintext secrets**: AWS's own example is `{\"Authorization\": \"Bearer <your-token>\"}`, with the Token Vault reference syntax (`${arn:...:apikeycredentialprovider/...}`) offered only as the better alternative. Tools are settable at **CreateHarness, UpdateHarness and per-`InvokeHarness`** \u2014 so a create-time inventory snapshot does not describe what the agent actually had at runtime. AISPM must inventory harness tools as first-class entities, flag `remote_mcp` targets pointing outside the org's approved MCP set, flag literal credential material in `headers`, and flag `agentcore_gateway` tools whose outbound auth is `None`",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-tools.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "AgentCore interactive shells: full terminal access with a documented command-content logging blind spot, plus a shell-takeover primitive",
        "description": "The `InvokeAgentRuntimeCommandShell` WebSocket API opens a persistent bash terminal inside the harness session's microVM. AWS states a principal holding it **\"has interactive command access to the harness session\"** and that the shell **\"can access customer-provided data, files, and secrets in the selected environment or on mounted filesystems\"** and reach any network destination the session can. The audit surface is the problem: **\"The service doesn't log terminal input or output\"** to CloudWatch, and **\"CloudTrail doesn't log terminal input or output\"** either \u2014 CloudTrail records only caller identity, timestamp, source IP and response status for the *connection*. Every command run against mounted EFS or bidirectionally-synced S3 filesystems is therefore invisible. Two further surfaces: authentication has a **second path that evaluates no IAM action at all** (bearer-token requests \"must satisfy the harness inbound authorizer instead,\" with the token carried inside a `Sec-WebSocket-Protocol` value for browser clients); and **opening a second connection with the same `shellId` replaces the first**, evicting the original client with close code `4000` while the shell's environment, working directory and running processes persist \u2014 a session-takeover primitive against a live agent terminal. Detached shells keep running and do not count toward the 10-active-shell limit. AIDR must add shell-connection events as an activity type keyed on `shellId` + `runtimeSessionId`, alert on `reconnected: true` and on `4000` evictions, and record that command content is structurally uncollectable from AWS-native logs \u2014 it can only be recovered from the harness runtime itself",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-command-shell.html",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Claude Compliance Activity Feed retroactively strips file names, document names and artifact titles",
        "description": "As of **September 24**, the Compliance API's Activity Feed no longer returns `filename` or `title` on file, project-document and artifact activities. The fields are **\"now always empty or omitted, including on activities recorded before this change\"** \u2014 this is not a going-forward change, it silently degrades **already-collected history**. Resolving a name from the ID on the activity now requires a **separate Compliance Access Key scoped `read:compliance_user_data`**, which is a different (and more sensitive) credential than the feed itself needs. Any Zenity detection, DLP correlation or investigation workflow that matched on document names in the Activity Feed is now matching on empty strings and will **fail silently rather than error**. AIDR must (a) treat empty `filename`/`title` as \"redacted,\" not \"no file,\" (b) add the ID\u2192name lookup as a second collection step with its own credential, and (c) re-check whether any existing rule keys on these fields. This is a coverage regression rather than a new feature, reported because it directly reduces an active detection surface",
        "source": "https://platform.claude.com/docs/en/manage-claude/compliance-activity-feed#understand-the-activity-object",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore `inline_function` tools execute client-side, and the obvious audit hook is documented as unreliable for them",
        "description": "An `inline_function` tool is defined with a schema on the harness but **\"executes on the client side, not on the harness VM.\"** The harness pauses with `stopReason: \"tool_use\"`, hands the call back to the caller's code, and resumes only when a matching `toolResult` arrives on the same `runtimeSessionId`. AWS names **human-in-the-loop approvals** as the primary pattern \u2014 meaning the approval gate for an agent's most sensitive actions frequently lives entirely outside AWS, in customer code, with no AWS-side record of what was approved or by whom. The harness holds the authoritative `toolUse` in session state and ignores a resent copy; a missing, duplicate, stale or replayed result cannot resume the handoff. AWS further warns to read the lifecycle-hooks caveat **\"before using an `after_tool_call` event for an inline function as an authorization or audit signal\"** \u2014 the natural instrumentation point is explicitly not trustworthy for this tool type. AISPM should inventory `inline_function` tools separately from server-side tools, record that their execution and any approval decision are off-platform, and flag harnesses whose privileged actions are gated only by an inline function",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-tools.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      }
    ]
  },
  {
    "date": "2026-09-25",
    "scan_time": "2026-09-25 09:16",
    "file": "report-2026-09-25.md",
    "platforms": [
      {
        "name": "Amazon Bedrock AgentCore (tier: primary) \u2014 carried audit debt cleared",
        "status": "changed",
        "details": [
          "**Harness persistent filesystem mounts.** A harness can mount three distinct persistent filesystem types at paths under `/mnt`: service-managed **session storage** (survives stop/resume for a `runtimeSessionId`, no VPC needed), an **Amazon EFS access point** (customer-owned, explicitly \"shared across sessions and agents\", up to 2 mounts, VPC required), and an **Amazon S3 Files access point** (up to 2 mounts, VPC required) whose mount path **syncs bidirectionally with the backing S3 bucket**. Files written to these mounts survive session termination and are visible to later invocations. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-environment.html",
          "**`InvokeAgentRuntimeCommand` \u2014 direct root shell into the harness microVM.** AWS documents this as deliberate out-of-band access: \"deterministic command execution with no model reasoning, no token cost.\" Commands run as **root (uid 0)**, and AWS states plainly that a `USER` directive in the customer's Dockerfile \"applies to the agent process only\" \u2014 `InvokeAgentRuntimeCommand` \"runs at a higher privilege level,\" and \"the IAM permission is the access gate, not the in-VM privilege level.\" Documented uses include cloning repos, installing packages, and pushing commits. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-environment.html",
          "**Harness memory: caller-supplied `actorId` is the isolation boundary, and managed memory is provisioned by default.** Memory events are scoped by `actorId` + `sessionId`, and `actorId` is passed by the caller on each `InvokeHarness` call. Long-term retrieval templates `actorId` directly into namespace paths (e.g. `/summary/{actorId}/{sessionId}/`). Retrieval is automatic on every invocation (`topK=10`, `relevanceScore=0.2` by default) and injects retrieved memories into context before the model reasons. Separately: creating a harness through the **service API** with the memory configuration omitted causes the service to **provision managed memory automatically** (the CLI defaults the other way, to disabled), and `DeleteHarness` cascade-deletes it unless `deleteManagedMemory=false`. BYO memory can be attached to and shared across multiple harnesses. Four long-term strategies: `SEMANTIC`, `SUMMARIZATION`, `USER_PREFERENCE`, `EPISODIC`. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-memory.html",
          "**Harness skill sources \u2014 four source types, fully enumerated.** The `skills` field (settable at create time, overridable per invocation, invoke-time entries appended after and winning on name collision) accepts: `awsSkills` (pre-built AWS-service skills from the AWS Agent Toolkit, glob-selected), `git` (clone **any public or private HTTPS Git repository**, sparse-checkout a subdirectory, private repos authenticated via a Token Vault `apikeycredentialprovider` ARN), `s3` (customer bucket, fetched with the execution role, \u22641 GB), and `path` (filesystem, baked into the container image *or installed at session start via `InvokeAgentRuntimeCommand`*). Skills follow the open **AgentSkills.io** standard \u2014 a `SKILL.md` plus optional `scripts/`, `references/` and `assets/` directories. Skills are fetched once per session and persist on disk across invocations. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-skills.html"
        ],
        "tier": "primary"
      },
      {
        "name": "Cursor (tier: watching)",
        "status": "error",
        "details": [
          "**`cursor.com/docs/rollouts` now exists** (404 on the 2026-09-24 scan). The docs page materially **corrects** the changelog text this report relied on yesterday: Rollouts **does not automatically revert** \u2014 on a detected regression it \"opens an issue, and notifies the author.\" It also documents the real integration surface: up to **200 monitored repositories**; version control across **GitHub, GitLab.com, Bitbucket Cloud and Origin**; observability via **Datadog and other tools connected over MCP**; Slack notifications to a DM or channel; and CI/CD integration by placing a **Cursor API key in CI secrets**. Check cadence after a deploy event is 20 minutes, 1 hour, 1 day and 3 days. At least one telemetry connection is required. Source: https://cursor.com/docs/rollouts"
        ],
        "tier": "primary"
      },
      {
        "name": "OpenAI Codex (tier: primary)",
        "status": "changed",
        "details": [
          "**Codex CLI 0.157.0 (2026-09-25)** \u2014 \"Enabled automatic background-server startup for eligible interactive sessions, with recovery choices when server settings are incompatible\"; \"Made `/import` available in remote sessions and local background-server sessions\"; an `f` shortcut to \"fork conversations open in another app, preserving drafts and queued prompts\"; GPT-6 Sol and Luna added to the catalog including **Amazon Bedrock** as a provider. Source: https://learn.chatgpt.com/docs/changelog"
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "AgentCore harness persistent filesystem mounts \u2014 three new resource types, one explicitly cross-agent",
        "description": "A harness can mount service-managed session storage, up to two **EFS access points**, and up to two **S3 Files access points** at paths under `/mnt`. AWS states the EFS mount is \"shared across sessions and agents\" \u2014 meaning two different agents, potentially with different execution roles and different data-classification levels, can be wired to the same access point and exchange files outside any modelled data flow. The S3 Files mount **syncs bidirectionally**, so anything an agent writes to its local mount path lands in the customer's S3 bucket automatically, and anything placed in that bucket becomes agent-readable input. Zenity's AgentCore inventory must add `filesystemConfigurations` as a first-class relationship \u2014 mapping each harness to its `sessionStorage`, `efsAccessPoint` and `s3FilesAccessPoint` mounts \u2014 and AISPM should raise a toxic-combination finding when one EFS or S3 Files access point is mounted by more than one agent or harness, and when an S3 Files-backed bucket is also writable by a non-agent principal (an injection path into agent context). Note `UpdateHarness` replaces the entire `filesystemConfigurations` list, so mount changes are not incremental and a drift-detection baseline must re-read the whole list",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-environment.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "`InvokeAgentRuntimeCommand` executes arbitrary shell as root inside the agent's microVM, bypassing the agent loop entirely",
        "description": "AWS documents this API as a deliberate out-of-band path with \"no model reasoning, no token cost\" \u2014 meaning it produces **no agent turn, no tool-use event and no model trace**. Commands run as **uid 0** regardless of a `USER` directive in the customer's container image, and AWS is explicit that \"the IAM permission is the access gate, not the in-VM privilege level.\" The documented use cases are themselves high-impact: clone a repository, install dependencies, run tests, **commit and push**, extract build artifacts, and install skills into `.agents/skills/` at session start. Any AIDR model that infers agent activity from invocation traces or model telemetry will see nothing at all for this path. Zenity must treat `bedrock-agentcore:InvokeAgentRuntimeCommand` as a distinct high-severity activity type sourced from CloudTrail rather than from agent traces, and AISPM must flag any principal holding it \u2014 it is effectively root on the agent host, and it is a separate IAM action from `InvokeHarness`",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-environment.html",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Harness memory isolation depends on a caller-supplied `actorId` string",
        "description": "Short-term and long-term memory are scoped by `actorId` + `sessionId`, and `actorId` is an ordinary request field the caller sets on `InvokeHarness`. Long-term retrieval templates it straight into the namespace path (`/summary/{actorId}/{sessionId}/`) and runs **automatically on every invocation**, injecting the top 10 semantically-matched records into context before the model reasons. If the calling application forwards a client-controlled identifier \u2014 or if the caller is a SigV4 principal, which per the harness security page gets **no per-user identity propagation at all** \u2014 then submitting another user's `actorId` retrieves that user's extracted facts, summaries, preferences and episodes into the attacker's session. This is a cross-tenant memory read reachable with no IAM change, and the injected content is also an indirect prompt-injection carrier (it is written by prior conversations). AIRT should add a probe that varies `actorId` across otherwise identical sessions and checks for retrieval leakage; AISPM should flag harnesses that are both memory-enabled and SigV4-invoked",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-memory.html",
        "module": "AIRT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore harness managed memory is silently auto-provisioned by the service API",
        "description": "Creating a harness through the service API with the memory configuration **omitted** causes AgentCore to provision a managed AgentCore Memory resource automatically \u2014 the AgentCore CLI defaults the opposite way (disabled). The result is a persistent store of conversation events and extracted long-term records (`SEMANTIC`, `SUMMARIZATION`, `USER_PREFERENCE`, `EPISODIC` strategies) that exists without the operator ever asking for it, and which cannot be deleted through the Memory APIs directly. Zenity's inventory must not assume \"no memory configured\" means \"no memory\" \u2014 it must resolve each harness's effective memory resource and mark implicitly-provisioned managed memory as an undeclared data store. Conversely, `DeleteHarness` cascade-deletes that memory by default unless `deleteManagedMemory=false` is passed, which matters for evidence retention. BYO memory attached to several harnesses is an additional cross-agent data-sharing edge to model",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-memory.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore harness skill sources \u2014 four source types including arbitrary Git clone at invoke time",
        "description": "This supplies the concrete entity model behind the per-invocation skill-override risk reported on 2026-09-24. The `skills` field accepts `awsSkills` (glob-selected AWS Agent Toolkit skills granting EC2/S3/Lambda/DynamoDB/CloudWatch/**IAM** operational knowledge), `git` (any public or private HTTPS repository, private repos authenticated with a Token Vault API-key credential provider holding a PAT), `s3` (execution-role-fetched, \u22641 GB), and `path` (baked into the image, or installed mid-session via `InvokeAgentRuntimeCommand`). Skills carry a `scripts/` directory alongside `SKILL.md` and conform to the open **AgentSkills.io** specification \u2014 the same cross-vendor plugin format Zenity already tracks \u2014 so a skill bundle is executable content, not just prose. Zenity should inventory skill attachments as a per-harness entity with the source type, URI/repo, and credential ARN recorded; flag invoke-time overrides as distinct from create-time defaults (invoke-time wins on name collision); and flag `awsSkills` globs that pull in IAM-operation skills",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-skills.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Codex CLI 0.157.0 starts a background server by default and opens `/import` to remote and background-server sessions",
        "description": "As of 0.157.0 the Codex CLI \"enable[s] automatic background-server startup for eligible interactive sessions\" \u2014 a long-lived local listener now comes up without the user opting in \u2014 and `/import` (pulling external content into a session) is newly available **in remote sessions and in local background-server sessions**. A new `f` shortcut forks a conversation that is open in another application, carrying over drafts and queued prompts, which moves conversation state across process boundaries. For endpoint coverage this means Codex's local footprint is no longer bounded by a foreground TUI process: ENDPOINT must detect the background server, and treat `/import` in a background-server session as an untrusted-content ingestion point into a tool-capable local agent",
        "source": "https://learn.chatgpt.com/docs/changelog",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      }
    ]
  },
  {
    "date": "2026-09-24",
    "scan_time": "2026-09-24 09:26",
    "file": "report-2026-09-24.md",
    "platforms": [
      {
        "name": "Cursor (tier: watching)",
        "status": "changed",
        "details": [
          "**Rollouts** \u2014 a new production-monitoring bot. It attaches a monitor to every pull request, reads the diff and the systems it touches, and writes a monitoring plan as a PR comment. It wakes on deploy events for the change's commit and runs that plan against the customer's logs, metrics and traces, tracking each environment separately and returning a per-environment verdict (verified healthy / regression detected / inconclusive). On a regression it names the suspected change, notifies the author, and \u2014 depending on configuration \u2014 **opens a revert PR or hands the finding to a cloud agent for a fix**. Integrations: source control (GitHub/Origin), the customer's CD system, and telemetry providers including Datadog, Grafana and Honeycomb. Teams and Enterprise plans. Source: https://cursor.com/changelog/rollouts-and-security-reviewer",
          "**Security Review** \u2014 a new security-agent family shipped alongside Rollouts. *Security Reviewer* reads every pull request in the context of the whole codebase and posts a single review comment reporting exploitable bugs (SQL/command/template injection, authn/authz bypass, secrets and credentials, SSRF, unsafe deserialization, vulnerable dependencies). A companion *Vulnerability Scanner* scans the codebase **at rest** for pre-existing issues. Teams can add custom codebase rules. Enabled per repository from the dashboard; draft PRs excluded. Source: https://cursor.com/docs/security-agents"
        ],
        "tier": "primary"
      },
      {
        "name": "Google Gemini Enterprise (tier: secondary)",
        "status": "changed",
        "details": [
          "**D&B Finance Analytics data store (Public Preview)** \u2014 new third-party data store. Beyond read access to company and credit reports, scores, filings and corporate ownership, it **supports actions**: creating and processing credit applications, and managing folders. This is the fifth connector-action expansion since 2026-08-21 and the second D&B data store in three days (D&B Risk Analytics GA'd 2026-09-22). Source: https://docs.cloud.google.com/gemini/enterprise/docs/connectors/d-and-b-commercial-graph"
        ],
        "tier": "primary"
      },
      {
        "name": "Amazon Bedrock AgentCore (tier: primary) \u2014 carried audit debt cleared",
        "status": "changed",
        "details": [
          "**Caller-controlled model configuration and skill sources.** The `model.additionalParams`, `apiBase` and `modelId` fields on `InvokeHarness` are passed through to the model provider **unvalidated and unfiltered** by AWS. The `skills` field can be overridden per invocation to point the harness at arbitrary S3 or Git sources, and the harness \"treats all skill content as trusted input.\" Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-security.html",
          "**Harness IAM permission model and `harness-endpoint` resource type.** Harness APIs require paired permissions on both the harness ARN and the underlying AgentCore Runtime (and Memory) resource \u2014 e.g. `InvokeHarness` needs both `bedrock-agentcore:InvokeHarness` and `bedrock-agentcore:InvokeAgentRuntime`. A distinct `arn:aws:bedrock-agentcore:<region>:<account>:harness/<id>/harness-endpoint/<name>` resource type exists with its own Create/Update/Delete/Get/List actions. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-security.html"
        ],
        "tier": "primary"
      },
      {
        "name": "Cognition Devin Desktop (tier: watching)",
        "status": "changed",
        "details": [
          "**v3.10.35 (2026-09-24): MCP servers advertised by ACP clients are now usable inside Devin sessions.** An MCP server the user never configured in Devin can now be reached by a Devin agent because a connected ACP (Agent Client Protocol) client exposes it. Source: https://docs.devin.ai/desktop/changelog"
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "Cursor Rollouts \u2014 an AI agent with production deploy, telemetry and revert authority",
        "description": "A new Cursor bot that attaches to every PR, ingests the customer's production logs, metrics and traces via Datadog/Grafana/Honeycomb integrations, and can autonomously open revert PRs or delegate a fix to a cloud agent. This introduces new inventory entities Zenity does not model: Rollouts monitors (one per PR), per-environment change-health verdicts, CD-system connections, and telemetry-provider connections. The toxic combination is concrete \u2014 an agent holding simultaneous read access to production observability data and write access to the source repository, able to revert code with no human in the loop depending on configuration. AISPM must discover Rollouts enablement per org, enumerate its connected deploy and telemetry providers, and flag configurations where autonomous revert is enabled",
        "source": "https://cursor.com/changelog/rollouts-and-security-reviewer",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Cursor Security Agents \u2014 Security Reviewer and Vulnerability Scanner",
        "description": "A new agent family that reads every pull request *in the context of the entire codebase* and, via the Vulnerability Scanner, scans the codebase at rest. New inventory entities: Security Reviewer agent configurations, per-repository enablement, custom codebase rules, and at-rest scan results. The security-relevant property is that these agents ingest whole-repository content \u2014 including secrets they are explicitly tasked with finding \u2014 and emit findings into PR comments, creating a new data-flow path out of the codebase. AISPM must inventory which repositories have security agents enabled and what custom rules are attached",
        "source": "https://cursor.com/docs/security-agents",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "AgentCore harness accepts unvalidated model configuration and caller-supplied skill sources",
        "description": "AWS documents that `additionalParams`, `apiBase` and `modelId` on `InvokeHarness` are passed to the model provider without validation. A caller can use LiteLLM's `aws_bedrock_runtime_endpoint` to redirect the signed request \u2014 **including the SigV4 signature and session credentials** \u2014 to an attacker-chosen endpoint, use OpenAI's `extra_headers` to override the `Authorization` header, attempt role escalation via `aws_role_name` + `sts:AssumeRole`, or redirect inference to a different model, region or provider entirely. Separately, the `skills` field can be overridden per invocation to fetch arbitrary markdown and scripts from any S3 bucket or Git repo, which the harness injects into agent context as trusted instructions. Both are first-class prompt-injection and credential-exfiltration vectors reachable by any principal that passes the IAM/JWT gate. AIRT should add probes for `additionalParams` endpoint redirection and per-invocation skill-source override; AISPM should flag harnesses whose execution role grants `sts:AssumeRole` or whose application forwards caller-supplied `model` and `skills` fields",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-security.html",
        "module": "AIRT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore `harness-endpoint` resource type and paired-permission IAM model",
        "description": "A distinct resource type `arn:aws:bedrock-agentcore:<region>:<account>:harness/<id>/harness-endpoint/<endpointName>` exists with its own `CreateHarnessEndpoint` / `UpdateHarnessEndpoint` / `DeleteHarnessEndpoint` / `GetHarnessEndpoint` / `ListHarnessEndpoints` actions, plus `ListHarnessVersions`. Harness control-plane calls require paired permissions on the harness *and* the underlying Runtime and Memory resources, meaning a principal appearing to hold only harness permissions may in fact carry Runtime and Memory authority. Zenity's AgentCore inventory must add `harness-endpoint` as an entity and model the paired-permission expansion when computing effective agent privilege. Note also that SigV4 callers do **not** get per-user identity propagation into downstream tool calls \u2014 only inbound OAuth (Bearer JWT) does \u2014 so any SigV4-invoked harness runs downstream tools under a shared service identity",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-security.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Gemini Enterprise D&B Finance Analytics data store with write actions",
        "description": "New Public Preview connector giving agents natural-language access to company and credit reports, scores, filings and corporate ownership, and \u2014 critically \u2014 **write actions** that create and process credit applications and manage folders. New inventory entities: the D&B Finance Analytics data store, its connector-action set, and the credit-application objects it can create. An agent with this connector can originate financial transactions, not merely read reference data. AISPM must discover the data store and enumerate its granted actions. **Doc gap:** Google has published no per-store page for either D&B store; the shared D&B connector overview does not enumerate actions, auth model or entity types, so the action set needs a follow-up sweep once documentation lands",
        "source": "https://docs.cloud.google.com/gemini/enterprise/docs/connectors/d-and-b-commercial-graph",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Devin Desktop exposes ACP-client MCP servers inside agent sessions",
        "description": "As of v3.10.35, MCP servers advertised by a connected ACP (Agent Client Protocol) client are usable from within Devin Desktop sessions. This is a transitive MCP-tool surface: a local agent gains tools that were never registered in Devin's own configuration, so an endpoint inventory built from Devin's MCP config alone will under-report the agent's real tool set. ENDPOINT coverage must resolve MCP servers reachable through ACP clients, not only those declared locally",
        "source": "https://docs.devin.ai/desktop/changelog",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      }
    ]
  },
  {
    "date": "2026-09-23",
    "scan_time": "2026-09-23 09:00",
    "file": "report-2026-09-23.md",
    "platforms": [
      {
        "name": "Claude Enterprise (Anthropic)",
        "status": "changed",
        "details": [
          "**Inline tool definitions \u2014 `tool_addition` by value (`inline-tools-2026-09-15` beta, September 22)** \u2014 a `tool_addition` block in a mid-conversation `role: \"system\"` message can now carry a **full tool definition by value** (name, description, `input_schema`) rather than naming a tool that was already declared in the request's `tools` array. The `tools` array never changes, so the prompt cache still hits. Re-sending a definition under the same name **replaces the tool's schema from that point onward**. Combined with the `mcp-client-2026-09-15` beta header, the same block can attach an **`mcp_toolset`** mid-conversation, and the response then carries `mcp_tool_listing` blocks enumerating that server's tools. Limits: 10,000 deferred tools, 10,000 tools defined after the first user message, 4 MB of post-first-user-message tool definitions. Computer-use tools cannot be defined inline. Supported on Claude Fable 5.1, Mythos 5.1, Opus 5, Opus 5.5 \u2014 on the Claude API, Amazon Bedrock and Google Cloud. Source: https://platform.claude.com/docs/en/build-with-claude/mid-conversation-system-messages",
          "**Claude Opus 5.5 (September 22)** \u2014 `claude-opus-5-5`, 1M context, 128k max output, always-on adaptive thinking; `thinking: {\"type\":\"disabled\"}` and `tool_choice` `any`/`tool` return 400. Computer use requires the `computer_toolset_20260801` toolset. Fast mode available as a research preview. *Model-inventory watchlist item; not raised as a coverage finding.* Source: https://platform.claude.com/docs/en/release-notes/api"
        ],
        "tier": "primary"
      },
      {
        "name": "Amazon Bedrock AgentCore",
        "status": "changed",
        "details": [
          "**Harness: Interactive shells (terminals)** \u2014 `InvokeAgentRuntimeCommandShell` opens a persistent, interactive **Bash PTY over WebSocket** inside the microVM running a harness session, at `wss://bedrock-agentcore.<region>.amazonaws.com/runtimes/<harness-arn>/ws/shells`. Environment variables, working directory, command history and running processes persist across inputs. A client can detach and **reconnect with the stored `shellId`**, replaying up to 256 KB of buffered output; up to **10 concurrent shells** per harness session (20 retained active+detached entries), 1-hour connection cap, 64 KB frames, 250 frames/sec. Auth is SigV4 (`bedrock-agentcore:InvokeAgentRuntimeCommandShell`), a SigV4 pre-signed URL, or a bearer token accepted by the harness inbound authorizer \u2014 including a **browser-friendly base64url bearer token passed through `Sec-WebSocket-Protocol`**. Works in the managed default environment and in a configured custom container. AWS's own guidance: the shell \"can access customer-provided data, files, and secrets in the selected environment or on mounted filesystems\" and \"connect to network destinations that the session can reach.\" **CloudTrail records the API call and CloudWatch gets connection metadata, but neither logs terminal input or output.** Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-command-shell.html"
        ],
        "tier": "primary"
      },
      {
        "name": "ChatGPT Enterprise (OpenAI)",
        "status": "changed",
        "details": [
          "**Codex CLI 0.156.0 (September 22)** \u2014 fullscreen TUI with search; **voice conversations enabled by default with bundled runtimes**; a **usage analytics dashboard at `/usage`** with plan history tracking; **daemon update management via `/daemon`**; worktree session filtering; **Windows sandbox changes \u2014 network policy support, DNS resolution, account handling, and integration with managed proxy routing**; **MCP policy consistency enforcement** and improved MCP credential handling; Code Mode tool metadata improvements and filesystem accessor bindings; refactored Guardian reviewer lifecycle; Mermaid/equation rendering. Source: https://learn.chatgpt.com/docs/changelog",
          "**GPT-6 Sol and GPT-6 Luna (September 22)** \u2014 new models in the API, Codex and ChatGPT Work, at ~50% lower API pricing than GPT-5.6; selectable from the Codex model picker as of CLI 0.156.1 (September 23). Follows GPT-6 Astra, already on the watchlist from the 2026-09-18 report. *Model-inventory watchlist item; not raised as a coverage finding.* Source: https://learn.chatgpt.com/docs/changelog"
        ],
        "tier": "primary"
      },
      {
        "name": "Google Gemini Enterprise",
        "status": "changed",
        "details": [
          "**D&B Risk Analytics data store \u2014 GA (September 22)** \u2014 a second Dun & Bradstreet federated data store alongside D&B Commercial Graph, targeted at KYB onboarding, counterparty due diligence, sanctions screening and risk assessment. It is a **connector with actions**, not a read-only index; Google's own guidance is to associate only one data store with the actions belonging to a single connector type. Available in `global`, `us` and `eu`; VPC Service Controls enforcement requires deleting and recreating existing data stores. Source: https://docs.cloud.google.com/gemini/enterprise/docs/connectors/d-and-b-commercial-graph"
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "Anthropic inline tool definitions \u2014 tools and MCP servers introduced mid-conversation, by value",
        "description": "A `tool_addition` block in a `role: \"system\"` message can now carry a complete tool definition, so a tool that Claude will call **need never appear in the request's `tools` array**. Every static analysis Zenity performs that reads an agent's declared toolset from the top of the request is now incomplete by design: the real tool surface is the union of `tools` plus every inline definition appended across the conversation, and a later definition under the same name **silently replaces the earlier schema**. Two consequences compound this. First, the same mechanism can attach an entire **`mcp_toolset` mid-conversation** (with `mcp-client-2026-09-15`), which means an MCP server \u2014 and the arbitrary tool list it returns in `mcp_tool_listing` \u2014 can enter an agent's reach after the conversation started and after any pre-flight policy check ran. Second, because the system-message block is *appended data*, an agent pipeline that builds messages from retrieved or user-influenced content gives an injection a route to **define its own tool** rather than merely persuade the model to misuse an existing one. AIRT needs probes for inline tool injection and mid-conversation MCP attachment; AISPM must treat an agent's tool inventory as conversation-scoped and evolving, parse `tool_addition` and `mcp_tool_listing` blocks as first-class inventory events, and alert on schema replacement of an already-known tool name",
        "source": "https://platform.claude.com/docs/en/build-with-claude/mid-conversation-system-messages",
        "module": "AIRT",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "AgentCore harness interactive shells (`InvokeAgentRuntimeCommandShell`)",
        "description": "A new WebSocket API that gives a caller a persistent, stateful Bash terminal inside the microVM running a harness agent \u2014 the same environment as the agent, with the same filesystem, mounted secrets and network reachability. AWS states plainly that **terminal input and output are not logged**: CloudTrail captures only the fact of the `InvokeAgentRuntimeCommandShell` call (caller identity, timestamp, source IP, resolved runtime, response status), and CloudWatch receives connection metadata only. Everything typed and everything returned is invisible to AWS-native logging, which makes this the least-observed execution path AgentCore has shipped. Detach-and-reconnect (`shellId` + runtime session ID) means a process started in one connection keeps running unattended and can be resumed later from a different client, and the browser-oriented **base64url bearer token over `Sec-WebSocket-Protocol`** path means a shell can be opened from a browser without SigV4 at all, authorised only by the harness inbound authorizer. Zenity must inventory which principals hold `bedrock-agentcore:InvokeAgentRuntimeCommandShell` and on which harness ARNs, treat any grant of it as a high-privilege finding (AWS's own security note says to restrict it to trusted principals and specific harness ARNs), surface harnesses whose inbound authorizer would accept a bearer-token shell connection, and ingest the CloudTrail event as an AIDR activity type \u2014 with the explicit caveat that content-level detection is not available and the compensating control has to be authorisation, not inspection",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-command-shell.html",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Codex CLI 0.156.0 \u2014 always-on voice with bundled runtimes, a local usage dashboard, and managed proxy routing in the Windows sandbox",
        "description": "Three changes alter what Codex is on a workstation. **Voice conversations are now enabled by default and ship with bundled runtimes**, which turns the local coding agent into a always-available microphone-driven input path with its own embedded speech runtime on disk \u2014 a new prompt-entry vector that leaves no text artifact and a new bundled binary to inventory. The **`/usage` analytics dashboard with plan history** is a locally reachable record of the agent's own consumption; useful as telemetry, and also a reconnaissance surface. The Windows sandbox now carries **network policy support, DNS resolution and integration with managed proxy routing**, meaning Codex's egress on Windows is governed by a configurable local policy object that ENDPOINT should read and assess rather than assume. Alongside these, **MCP policy consistency enforcement** and improved MCP credential handling change how locally configured MCP servers are admitted, and **Code Mode tool metadata plus filesystem accessor bindings** expand what the agent can reach on the filesystem. This is an extension of the Codex daemon/worktree endpoint object already reported 2026-09-18, not a new product, hence MEDIUM",
        "source": "https://learn.chatgpt.com/docs/changelog",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Gemini Enterprise D&B Risk Analytics data store (GA)",
        "description": "A second Dun & Bradstreet connector reaches GA, and unlike a plain search index it is a **connector with actions** \u2014 meaning a Gemini Enterprise agent can invoke operations against an external commercial-risk service, not merely read from it. The declared use cases (KYB onboarding, counterparty due diligence, sanctions screening, risk decisioning) put it directly in a regulated decision path: an agent grounded on this store is producing outputs that feed onboarding and credit decisions, and the store itself becomes a route by which internal counterparty identifiers leave the tenant for a third-party data provider. AISPM must add D&B Risk Analytics as an inventory entity alongside D&B Commercial Graph, enumerate the actions registered for it (the action set is the privilege, not the connector), capture its region binding (`global`/`us`/`eu`) as a data-residency attribute, and flag agents that hold it as a grounding source in a compliance-relevant workflow",
        "source": "https://docs.cloud.google.com/gemini/enterprise/docs/connectors/d-and-b-commercial-graph",
        "module": "AISPM",
        "action": "New Coverage Required"
      }
    ]
  },
  {
    "date": "2026-09-22",
    "scan_time": "2026-09-22 10:10",
    "file": "report-2026-09-22.md",
    "platforms": [
      {
        "name": "Amazon Bedrock AgentCore",
        "status": "changed",
        "details": [
          "**Harness: Lifecycle hooks** \u2014 hooks fire at `before_invocation`, `before_tool_call`, `after_tool_call` and `after_invocation`, including for inline-function tool calls. An AWS Lambda target returns a synchronous `allow` or `deny` that can stop an invocation or skip a tool call; Amazon SNS and Amazon EventBridge targets receive non-blocking notifications. `InvokeHarness` emits a `hookEvent` for each configured hook that fires. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-lifecycle-hooks.html",
          "**Harness: Custom OpenAI-compatible endpoints** \u2014 OpenAI model configurations now accept an optional `apiBase` field, routing direct OpenAI requests through a customer-specified gateway, proxy, self-hosted endpoint or regional endpoint. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-models.html"
        ],
        "tier": "primary"
      },
      {
        "name": "Claude Enterprise (Anthropic)",
        "status": "changed",
        "details": [
          "**Life Sciences Verification Program (September 17, 2026)** \u2014 a vetting-gated entitlement that replaces an organisation's biology-related safety classifiers with a more permissive set. Two tiers: *Standard Use* (Mythos, Opus and Sonnet with classifiers tuned permissive for science tasks) and *High-risk Use*, an add-on that **removes all safeguards blocking life-sciences requests** for a named project. Cyber classifiers and other safeguards are retained. Grants are annual (Standard) or bi-annual (High-risk), scoped to declared use cases, and enforcement shifts from real-time blocking to offline pattern analysis. Source: https://www.anthropic.com/news/life-sciences-verification-program"
        ],
        "tier": "primary"
      },
      {
        "name": "Google Gemini Enterprise",
        "status": "changed",
        "details": [
          "**Transfer agent ownership (September 21, 2026)** \u2014 administrators holding `roles/discoveryengine.agentspaceAdmin` or `roles/discoveryengine.admin` can reassign ownership of a shared agent to another user, including themselves. Shared agents only; the new owner becomes sole owner and the previous owner is downgraded to `agentUser`. Targets may be Google accounts or Workforce Identity Federation principals. Any schedule or event trigger on the agent is marked disabled by the transfer and must be re-enabled by the new owner. Source: https://docs.cloud.google.com/gemini/enterprise/docs/share-custom-agents"
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "AgentCore harness lifecycle hooks",
        "description": "A new synchronous interception and control surface inside the AgentCore agent loop. Four hook points (`before_invocation`, `before_tool_call`, `after_tool_call`, `after_invocation`) can each bind a Lambda that returns `allow`/`deny` and can halt an invocation or suppress a tool call, plus SNS/EventBridge fan-out targets. This introduces new inventory entities (harness hook configurations, their Lambda/SNS/EventBridge targets) and a new activity type \u2014 the `hookEvent` records emitted by `InvokeHarness` \u2014 which is a first-class detection feed for what an AgentCore agent actually invoked. It is also a control plane an attacker with harness-config write access can neuter or repurpose: replacing a `deny` Lambda silently removes the customer's own tool-call gating, and a `before_tool_call` Lambda sees every tool argument, making it an exfiltration point. Zenity must inventory hook configurations, alert on hooks removed or repointed, and ingest `hookEvent` as an AIDR activity source",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-lifecycle-hooks.html",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Anthropic Life Sciences Verification Program \u2014 entitlement-based safeguard removal",
        "description": "A verified organisation's Claude deployment can run with biology safety classifiers relaxed (Standard Use) or, at the High-risk tier, with all life-sciences-blocking safeguards removed for a named project. Guardrail behaviour is therefore a property of the *entitlement attached to the workspace/API key*, not of the model \u2014 the same `claude-mythos-5`/Opus/Sonnet model ID behaves differently for an entitled org, and Anthropic explicitly moves that tier from real-time blocking to offline pattern analysis. Red-team probes that infer safety posture from refusals will pass falsely against an entitled deployment. AIRT needs a bio-domain probe set, and AISPM needs to record the LSVP entitlement and tier as a property of the Anthropic connection so an unexpected High-risk grant is a finding. Directly parallels the Mythos-5 safeguard-lifting pattern already tracked",
        "source": "https://www.anthropic.com/news/life-sciences-verification-program",
        "module": "AIRT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore harness custom OpenAI endpoint (`apiBase`)",
        "description": "OpenAI model configs in the AgentCore harness now take an `apiBase` pointing at any OpenAI-compatible gateway, proxy, self-hosted or regional endpoint. This is a new outbound model-traffic path that leaves AWS-observed inference entirely \u2014 every prompt, tool result and completion for that agent can be routed to an arbitrary third-party host set by a single config field, with no code change and no new IAM resource. AISPM must surface `apiBase` per harness configuration, treat a non-default value as an unmonitored data-egress edge, and flag agents whose model traffic bypasses Bedrock",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-models.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Gemini Enterprise agent ownership transfer",
        "description": "A new administrative action that reassigns the sole owner of a shared agent, demoting the previous owner to `agentUser`, and that accepts Workforce Identity Federation principals as well as Google accounts. This mutates an inventory relationship Zenity already models (agent \u2192 owner) through a path that is not agent creation, so ownership drift will go unobserved unless the transfer is tracked; it is also a plausible privilege-consolidation move \u2014 an admin can make themselves the owner of any shared agent in the tenant. The transfer additionally disables the agent's schedule and event triggers, silently changing autonomous-execution posture. AISPM should treat owner change on an existing agent, and trigger-state change caused by it, as distinct monitored events",
        "source": "https://docs.cloud.google.com/gemini/enterprise/docs/share-custom-agents",
        "module": "AISPM",
        "action": "New Coverage Required"
      }
    ]
  },
  {
    "date": "2026-09-21",
    "scan_time": "2026-09-21 11:09",
    "file": "report-2026-09-21.md",
    "platforms": [],
    "findings": []
  },
  {
    "date": "2026-09-20",
    "scan_time": "2026-09-20 10:46",
    "file": "report-2026-09-20.md",
    "platforms": [
      {
        "name": "Google Gemini Enterprise (secondary)",
        "status": "changed",
        "details": [
          "**New actions for Microsoft OneDrive, Outlook, SharePoint and Teams data stores (September 18, Public Preview).** Four Microsoft connectors gained write and management operations. OneDrive: copy folder, move file, move folder, rename file, rename folder, **share file or folder**, update file properties. Outlook: create calendar, RSVP to event, update calendar. SharePoint: create list item, discard check-out, get list fields, get list item, list lists, **share resource**, update file properties, update list, update list item, update page. Teams: **add member to channel**, create channel, create chat, create schedule, create time-off entry, update channel, update channel message, update chat, update chat message, update time-off entry. Source: https://docs.cloud.google.com/gemini/enterprise/docs/manage-actions",
          "**Workflow Builder GA (September 3) \u2014 previously unreported.** Formerly Agent Designer. A no-code/low-code surface in the Gemini Enterprise web app where **non-admin end users** create chat agents and multi-step workflows from natural-language prompts or a visual flow editor, connect them to data sources, tools and **MCP servers**, schedule recurring executions, and publish and share them with users or groups. Source: https://docs.cloud.google.com/gemini/enterprise/docs/workflow-builder"
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "Gemini Enterprise can now write into Microsoft 365 \u2014 share OneDrive files, share SharePoint resources, and add members to Teams channels",
        "description": "The September 18 preview adds action sets to the Microsoft OneDrive, Outlook, SharePoint and Teams data stores, converting four read connectors into read-write ones. Three of the new actions are permission-mutating rather than content-mutating, and those are the ones that matter: OneDrive **\"share file or folder\"**, SharePoint **\"share resource\"**, and Teams **\"add member to channel\"**. An agent holding these can change who has access to Microsoft 365 data from outside Microsoft's control plane entirely \u2014 the grant is made by a Google-side principal acting through a stored connector credential, so it lands in the tenant as a sharing event without a corresponding Entra agent identity, Copilot Studio agent, or Foundry autopilot behind it. Every Microsoft-side agent-governance surface Zenity tracks (Entra Agent ID, Agent 365, Copilot Studio DLP, Defender Security for AI) sees nothing here, because the actor is not a Microsoft agent. The remaining actions broaden the blast radius in ordinary ways worth inventorying: SharePoint list item and page writes, OneDrive file moves and renames, Outlook calendar creation and event RSVP, and a full Teams management set including channel creation, chat creation, message updates and **time-off entry creation** (a Shifts/workforce-data write, which is employee data). AISPM should model connector actions as a first-class capability on each Gemini Enterprise data store \u2014 an inventory that records \"SharePoint connector: connected\" without recording *which action set is enabled* will now understate reach by an order of magnitude \u2014 and should flag any data store where sharing-class actions (`share file or folder`, `share resource`, `add member to channel`) are enabled, since those let an agent expand access rather than merely consume it. AIDR should treat Gemini-originated M365 writes as a distinct activity class and correlate the Google-side action against the Microsoft-side audit event, because neither log alone attributes the change to an agent. This is the fourth consecutive scan (2026-08-21, 2026-08-30, 2026-09-02, 2026-09-18) in which Google expanded actions without expanding the connector list; the connector inventory looks static while the capability surface does not",
        "source": "https://docs.cloud.google.com/gemini/enterprise/docs/manage-actions",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Gemini Enterprise Workflow Builder: end users build, schedule and share agents and workflows with MCP server connections \u2014 GA'd September 3, missed on the last scan",
        "description": "Workflow Builder (formerly Agent Designer) reached general availability as \"an interactive no-code, low-code platform for creating, managing, and turning on chat agents and workflows in your Gemini Enterprise web app.\" Four properties make it an inventory requirement rather than a UI change. **First, the builder is not admin-gated.** Google's own framing is that it \"empowers employees and non-admin users to build and deploy chat agents and workflows\" \u2014 the constraint is that available tools are \"limited to the data connectors, tools, and permissions provisioned by the Gemini Enterprise administrator.\" That is a real ceiling, but it is a *tenant-wide* ceiling: any connector an admin provisions becomes composable by every end user, so the admin controls the palette and the user controls the composition. Combined with the connector-action finding above, an end user can now assemble a workflow that shares SharePoint resources or adds Teams channel members without any per-workflow review. **Second, workflows connect to MCP servers** \u2014 a documented first-class capability, meaning user-authored workflows reach arbitrary external tools through a protocol Zenity already treats as a monitored surface elsewhere. **Third, workflows run on a schedule**, with documented chat-agent execution scheduling. A scheduled workflow is an unattended agent: it acts with no user present, on a recurring cadence, which removes the human-in-the-loop assumption underpinning most interactive-assistant risk models. **Fourth, workflows are publishable and shareable** to specific users or groups, so one user's construction becomes an organizational asset with its own distribution graph. AISPM should discover Workflow Builder chat agents and workflows as inventory entities distinct from Gemini Enterprise's admin-registered agents; record each one's **author** (and whether that author is a non-admin), its connector and tool grants, its MCP server connections, its execution schedule, and its share scope; and flag scheduled workflows holding write-capable or sharing-capable connector actions, workflows connected to MCP servers not on an approved list, and workflows shared organization-wide. This is shadow-agent creation as a shipped product feature, and it pairs directly with the Gemini Enterprise **Projects** entity reported on 2026-09-18 \u2014 Projects gives users a self-assembled knowledge base, Workflow Builder gives them a self-assembled actor, and the two are designed to connect. Noting the correction: this GA'd on September 3 and was visible in the release notes read on 2026-09-18, where the adjacent agent-observability item was reported and this one was not",
        "source": "https://docs.cloud.google.com/gemini/enterprise/docs/workflow-builder",
        "module": "AISPM",
        "action": "New Coverage Required"
      }
    ]
  }
]