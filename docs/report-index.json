[
  {
    "date": "2026-09-20",
    "scan_time": "2026-09-20 10:46",
    "file": "report-2026-09-20.md",
    "platforms": [
      {
        "name": "Google Gemini Enterprise (secondary)",
        "status": "changed",
        "details": [
          "**New actions for Microsoft OneDrive, Outlook, SharePoint and Teams data stores (September 18, Public Preview).** Four Microsoft connectors gained write and management operations. OneDrive: copy folder, move file, move folder, rename file, rename folder, **share file or folder**, update file properties. Outlook: create calendar, RSVP to event, update calendar. SharePoint: create list item, discard check-out, get list fields, get list item, list lists, **share resource**, update file properties, update list, update list item, update page. Teams: **add member to channel**, create channel, create chat, create schedule, create time-off entry, update channel, update channel message, update chat, update chat message, update time-off entry. Source: https://docs.cloud.google.com/gemini/enterprise/docs/manage-actions",
          "**Workflow Builder GA (September 3) \u2014 previously unreported.** Formerly Agent Designer. A no-code/low-code surface in the Gemini Enterprise web app where **non-admin end users** create chat agents and multi-step workflows from natural-language prompts or a visual flow editor, connect them to data sources, tools and **MCP servers**, schedule recurring executions, and publish and share them with users or groups. Source: https://docs.cloud.google.com/gemini/enterprise/docs/workflow-builder"
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "Gemini Enterprise can now write into Microsoft 365 \u2014 share OneDrive files, share SharePoint resources, and add members to Teams channels",
        "description": "The September 18 preview adds action sets to the Microsoft OneDrive, Outlook, SharePoint and Teams data stores, converting four read connectors into read-write ones. Three of the new actions are permission-mutating rather than content-mutating, and those are the ones that matter: OneDrive **\"share file or folder\"**, SharePoint **\"share resource\"**, and Teams **\"add member to channel\"**. An agent holding these can change who has access to Microsoft 365 data from outside Microsoft's control plane entirely \u2014 the grant is made by a Google-side principal acting through a stored connector credential, so it lands in the tenant as a sharing event without a corresponding Entra agent identity, Copilot Studio agent, or Foundry autopilot behind it. Every Microsoft-side agent-governance surface Zenity tracks (Entra Agent ID, Agent 365, Copilot Studio DLP, Defender Security for AI) sees nothing here, because the actor is not a Microsoft agent. The remaining actions broaden the blast radius in ordinary ways worth inventorying: SharePoint list item and page writes, OneDrive file moves and renames, Outlook calendar creation and event RSVP, and a full Teams management set including channel creation, chat creation, message updates and **time-off entry creation** (a Shifts/workforce-data write, which is employee data). AISPM should model connector actions as a first-class capability on each Gemini Enterprise data store \u2014 an inventory that records \"SharePoint connector: connected\" without recording *which action set is enabled* will now understate reach by an order of magnitude \u2014 and should flag any data store where sharing-class actions (`share file or folder`, `share resource`, `add member to channel`) are enabled, since those let an agent expand access rather than merely consume it. AIDR should treat Gemini-originated M365 writes as a distinct activity class and correlate the Google-side action against the Microsoft-side audit event, because neither log alone attributes the change to an agent. This is the fourth consecutive scan (2026-08-21, 2026-08-30, 2026-09-02, 2026-09-18) in which Google expanded actions without expanding the connector list; the connector inventory looks static while the capability surface does not",
        "source": "https://docs.cloud.google.com/gemini/enterprise/docs/manage-actions",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Gemini Enterprise Workflow Builder: end users build, schedule and share agents and workflows with MCP server connections \u2014 GA'd September 3, missed on the last scan",
        "description": "Workflow Builder (formerly Agent Designer) reached general availability as \"an interactive no-code, low-code platform for creating, managing, and turning on chat agents and workflows in your Gemini Enterprise web app.\" Four properties make it an inventory requirement rather than a UI change. **First, the builder is not admin-gated.** Google's own framing is that it \"empowers employees and non-admin users to build and deploy chat agents and workflows\" \u2014 the constraint is that available tools are \"limited to the data connectors, tools, and permissions provisioned by the Gemini Enterprise administrator.\" That is a real ceiling, but it is a *tenant-wide* ceiling: any connector an admin provisions becomes composable by every end user, so the admin controls the palette and the user controls the composition. Combined with the connector-action finding above, an end user can now assemble a workflow that shares SharePoint resources or adds Teams channel members without any per-workflow review. **Second, workflows connect to MCP servers** \u2014 a documented first-class capability, meaning user-authored workflows reach arbitrary external tools through a protocol Zenity already treats as a monitored surface elsewhere. **Third, workflows run on a schedule**, with documented chat-agent execution scheduling. A scheduled workflow is an unattended agent: it acts with no user present, on a recurring cadence, which removes the human-in-the-loop assumption underpinning most interactive-assistant risk models. **Fourth, workflows are publishable and shareable** to specific users or groups, so one user's construction becomes an organizational asset with its own distribution graph. AISPM should discover Workflow Builder chat agents and workflows as inventory entities distinct from Gemini Enterprise's admin-registered agents; record each one's **author** (and whether that author is a non-admin), its connector and tool grants, its MCP server connections, its execution schedule, and its share scope; and flag scheduled workflows holding write-capable or sharing-capable connector actions, workflows connected to MCP servers not on an approved list, and workflows shared organization-wide. This is shadow-agent creation as a shipped product feature, and it pairs directly with the Gemini Enterprise **Projects** entity reported on 2026-09-18 \u2014 Projects gives users a self-assembled knowledge base, Workflow Builder gives them a self-assembled actor, and the two are designed to connect. Noting the correction: this GA'd on September 3 and was visible in the release notes read on 2026-09-18, where the adjacent agent-observability item was reported and this one was not",
        "source": "https://docs.cloud.google.com/gemini/enterprise/docs/workflow-builder",
        "module": "AISPM",
        "action": "New Coverage Required"
      }
    ]
  },
  {
    "date": "2026-09-18",
    "scan_time": "2026-09-18 08:26",
    "file": "report-2026-09-18.md",
    "platforms": [
      {
        "name": "Microsoft Foundry \u2014 autopilots (`ms.date` 2026-08-25), missed in the 09-02 pass",
        "status": "changed",
        "details": [
          "**Autopilot: a third Foundry agent type defined by holding an Entra agent user account.** \"An autopilot is a type of agent that works under its own identity as a persistent, named member of your organization.\" Every Foundry agent has an Entra **agent identity** (a service principal) from creation; an autopilot additionally has an Entra **agent user account** (a user object) carrying \"its own email, calendar, OneDrive, Teams presence, and a place in the organization chart,\" plus a **manager** who answers for it. Microsoft's three-type table: **Assistive** (agent identity + signed-in user context, acts within that user's permissions), **Background service** (agent identity only, app-only permissions, cannot perform M365 actions), **Autopilot** (agent identity **and** agent user account, acts as itself in M365 including group chats). Source: https://learn.microsoft.com/en-us/azure/foundry/agents/concepts/autopilot-overview",
          "**Blueprint-to-instance fan-out.** \"You don't build an autopilot. You build a blueprint that teams use to create their own autopilot instances, each with its own identity and its own team-scoped access.\" One blueprint \u2192 many hired instances, each with **its own agent identity and its own agent user account**. Policy is a separate layer: \"Configure policy once and every instance inherits it. Update the blueprint and every instance updates. Block the blueprint and every instance stops.\" Only **Foundry hosted agents** can be published as autopilot blueprints. Three patterns: **group**, **company-wide**, and **personal** autopilots. Source: same",
          "**Explicit tool-versus-data separation.** \"Holding a tool isn't the same as holding access to the data behind it. Giving an autopilot a SharePoint tool doesn't give it any SharePoint site, and giving it a mailbox doesn't give it the ability to send mail.\" Source: same",
          "*(Also in the same August batch and still held as extensions: agent optimizer cost/token accounting, autopilot lifecycle, human-in-the-loop approval steps, toolbox network isolation.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Claude Enterprise (Anthropic) \u2014 September 3 to September 18",
        "status": "error",
        "details": [
          "**`ant apply` \u2014 infrastructure-as-code for agent fleets (CLI \u2265 1.30.0, September 3).** Creates and updates **agents, environments, skills, memory stores and deployments** from files in a repository. Agents are Markdown (frontmatter = config, body = system prompt); environments and memory stores are YAML; deployments are Markdown whose prose \"becomes the message that starts each session\"; a skill is a directory with `SKILL.md` uploaded as one bundle. Kind is inferred from a `type` field, the containing directory (`agents/`, `environments/`, `memory_stores/`, `deployments/`), or a filename prefix. Resources reference each other **by relative path** and `ant apply` resolves them in dependency order. **A skill reference can be a GitHub URL** (`https://github.com/<owner>/<repo>/tree/<branch>/<dir>`) that `ant apply` downloads and uploads, pinned to the resolved commit until `--upgrade`. State lives in a committed **`claude-lock.json`** recording each resource's kind, ID, version and two hashes (what was sent, what the API returned) plus the bound `organization_id` and `workspace_id`. CI guidance is `ant apply --yes .` on the default branch, authenticated with **Workload Identity Federation**, and the command \"refuses credentials that resolve to any other organization or workspace.\" `--force` overwrites out-of-band edits; `--prune` archives resources whose files were deleted. Source: https://platform.claude.com/docs/en/cli-sdks-libraries/cli/apply",
          "**`auto` permission policy \u2014 server-side per-call tool authorization (September 10).** A third policy alongside `always_allow` and `always_ask`. \"The server evaluates each call before it runs\u2026 Because the evaluation considers the tool, the call's input, and the session's content up to that point, the server can treat two calls to the same tool differently.\" Three outcomes: the call runs; the call is **denied** (the agent gets `Permission to use {tool_name} has been denied.` with `is_error: true`, and \"your client cannot override the denial\"); or the call **pauses** for approval. Defaults are unchanged \u2014 agent toolset `always_allow`, MCP toolsets `always_ask`, and \"no toolset uses `auto` by default.\" Anthropic's own warning: \"`auto` is not a human checkpoint. If the server determines that a call is safe, the call runs before anyone sees it, and its effects might not be reversible.\" Source: https://platform.claude.com/docs/en/managed-agents/permission-policies",
          "**New event fields on every tool call.** Under **any** policy, `agent.tool_use` and `agent.mcp_tool_use` events now carry **`evaluated_permission`** (`\"allow\"` / `\"ask\"` / `\"deny\"`) and, in most cases, an **`evaluation`** object whose `type` names the resolving policy; under `auto` it nests the server's determination plus a **`reason_code`** (`high_risk`, `indeterminate`). `evaluation` is **absent** when the agent names a tool that isn't enabled (denied without policy evaluation) and on events recorded before the field existed. `agent.custom_tool_use` carries neither field. Source: same",
          "**`ant beta:sessions connect` (September 10)** \u2014 attaches a terminal to a running Managed Agents session, shows waiting tool calls and sends the `user.tool_confirmation` event interactively; `--web` serves a session viewer locally. Source: https://platform.claude.com/docs/en/cli-sdks-libraries/cli/sessions-connect",
          "**Compact-on-demand (beta, September 14)** \u2014 a top-level **`compaction`** parameter on the Messages API under the `compact-2026-09-04` beta header. The API returns a **signed `compaction` block** summarizing prior messages, and can \"keep recent turns word-for-word while preserving thinking validity.\" Source: https://platform.claude.com/docs/en/build-with-claude/compaction",
          "**Compliance API: Claude in Chrome sessions (beta, September 18).** The local session endpoints now return transcripts with **`product_surface: claude_in_chrome`**. The covered surface list is now **Cowork, Claude Code, Claude Science, Claude for Microsoft 365, and Claude in Chrome**; Cowork and Claude Code are stable, the other three are beta. No new key, scope, setting or client update is required \u2014 the existing Compliance Access Key with `read:compliance_user_data` works. Source: https://platform.claude.com/docs/en/manage-claude/compliance-sessions"
        ],
        "tier": "primary"
      },
      {
        "name": "Amazon Bedrock AgentCore \u2014 September release notes",
        "status": "changed",
        "details": [
          "**Consent Portal for AgentCore Identity.** \"A consent portal is a hosted, AWS-managed portal that authenticates your end users to an OpenID Connect (OIDC) identity provider (IdP) and gathers their consent before your agent accesses a downstream resource on their behalf.\" Each portal **attaches to a single AgentCore Gateway** (its source) and uses an **OAuth2 credential provider** referencing the same IdP the gateway's inbound JWT authorizer trusts. The gateway must use **JWT inbound authentication** and the IdP's permitted scopes must include `openid`. You direct users to the portal's **`portalUrl`**. \"The portal keeps the OAuth flow server-side: the browser never holds a token.\" Managed with **create, get, list, update and delete consent-portal operations**, plus a **consent portal execution role** and configurable **consent portal targets**. Sources: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/identity-consent-portal.html and https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/release-notes.html",
          "**Evaluations: TypeScript agent framework support** \u2014 AgentCore Evaluations now evaluates agents built with the TypeScript versions of Strands Agents, LangGraph and OpenAI Agents, plus the **Vercel AI SDK** (TypeScript-only). Held as an extension of the already-reported evaluations surface, but note the Vercel AI SDK is a framework this monitor has not previously seen named in an AWS evaluation scope. Source: same"
        ],
        "tier": "primary"
      },
      {
        "name": "ServiceNow \u2014 Zurich release notes now render (updated 2026-08-07)",
        "status": "changed",
        "details": [
          "**Build Agent** \u2014 creates applications inside **ServiceNow Studio** from natural language, now with \"additional **Model Context Protocol (MCP)** support,\" the ability to \"create apps and newly supported metadata **in the global scope**,\" and external content search without context switching. Source: https://www.servicenow.com/docs/r/zurich/release-notes/rn-summary-highlights.html",
          "**AI Control Tower, redesigned** \u2014 natural-language governance, **automated deduplication** of redundant AI assets, **AI Service Graph Connectors** discovering assets across AWS, Microsoft and GCP, **kill switch** protocols to contain rogue agents, and **publishing managed agents to external registries**. Source: same",
          "**Machine Identity Access Controls** \u2014 \"Enforce access to data via REST or SOAP endpoints\" for non-human identities. Source: same",
          "**AI Agent Studio / Autonomous Workforce** \u2014 AI specialist configuration and deployment for coordinated agentic AI, plus **Desktop Actions** automation with an AI-powered recording mode. Source: same"
        ],
        "tier": "primary"
      },
      {
        "name": "Google Gemini Enterprise \u2014 release notes advanced to 2026-09-17",
        "status": "error",
        "details": [
          "**Projects (September 4)** \u2014 \"build a dedicated knowledge base for your own work or to collaborate with your team.\" A project holds up to **50 files and linked documents** drawn from local uploads (PDF, DOCX, PPTX, XLSX, CSV, HTML, TXT), **Google Drive files and folders**, free-text notes, and **third-party connectors such as Microsoft SharePoint**. Only owners can invite, remove or delete; **invited members receive Editor permissions**. Members see only the files they already have permission to access; linking Drive content \"doesn't change their original sharing permissions\"; locally uploaded files are visible and deletable by **all** members; chats stay private per user. Source: https://docs.cloud.google.com/gemini/enterprise/docs/projects",
          "**Google Cloud Marketplace semantic search for agent discovery (September 8)** \u2014 agents in the Marketplace are now discoverable by semantic search. Source: https://docs.cloud.google.com/gemini/enterprise/docs/release-notes",
          "**Monday federated data store (GA) and four new preview connectors (September 4)** \u2014 CoinDesk, Relativity, Streak, Superhuman Mail \u2014 plus **new actions** for AirOps, Airtable and Zoho Desk. **Five more preview connectors on August 28**: Campfire, Clay, CourtListener, Daloopa, MSCI. Source: same",
          "**Agent observability (September 3)** \u2014 latency and error-rate views for agents. **Slack app (September 10)** gained channel mentions and multi-turn conversation support. **Voice input for chat (September 17)**. Source: same"
        ],
        "tier": "primary"
      },
      {
        "name": "Salesforce \u2014 Dreamforce 2026, September 15-17",
        "status": "changed",
        "details": [
          "**AIforce (September 15)** \u2014 announced as the reframing of the platform, bringing enterprise capabilities across interfaces. Source: https://www.salesforce.com/news/",
          "**Koa (September 15)** \u2014 \"Salesforce's first CRM reasoning model,\" built by post-training **NVIDIA Nemotron 3 Super** on a proprietary synthetic dataset modeled on ~27 years of CRM deployments. \"Purpose-built to help agents reason through complex, multistep workflows and use the right tools to get work done.\" No customer data was used in training. **Available to select pilot customers now in Agentforce; GA expected winter 2026 in U.S. regions.** Source: https://www.salesforce.com/news/press-releases/2026/09/15/koa-reasoning-model/",
          "**Missionforce expansion (September 16)** \u2014 partnerships with NVIDIA and OpenAI for purpose-built government and defense agents. **Agentforce Coworker** rolled out globally by Adecco across 40 countries (September 15). Source: https://www.salesforce.com/news/"
        ],
        "tier": "primary"
      },
      {
        "name": "Cursor \u2014 September 2 and September 10",
        "status": "changed",
        "details": [
          "**Cursor Projects (September 10)** \u2014 a **coordinator agent** that \"delegates tasks to thousands of subagents in parallel,\" maintains shared context **across cloud and local machines**, and supports **subscriptions** that monitor Slack channels and run scheduled tasks. Source: https://cursor.com/changelog",
          "**Self-Hosted Machines (September 2)** \u2014 infrastructure for \"keeping tool execution within internal networks,\" with dynamic pool scheduling and autoscaling, **computer use on Linux and Mac with desktop automation**, and integrations with AWS Lambda, Coder, Cloudflare, Daytona, Modal, Namespace, Vercel and E2B. Source: same"
        ],
        "tier": "primary"
      },
      {
        "name": "ChatGPT Enterprise (OpenAI) \u2014 Codex CLI 0.154.0 and 0.155.0",
        "status": "changed",
        "details": [
          "**Codex CLI 0.155.0 (September 17)** \u2014 experimental **voice conversations** with live transcripts and microphone controls; **Touch ID verification for MCP requests** on supported Macs; **configurable daemon update schedules with thread recovery after restarts**; **AWS credential sourcing from configured commands for Amazon Bedrock**; task hiding, archiving and deletion in the agents overview. Source: https://learn.chatgpt.com/docs/changelog",
          "**Codex CLI 0.154.0 (September 9)** \u2014 **GPT-6-Astra** available in the model picker **and in Amazon Bedrock catalogs**; experimental **worktree support** for isolated checkouts in new and forked sessions; a **Windows background server with daemon lifecycle commands**; inline answers while Codex continues working. Source: same"
        ],
        "tier": "primary"
      },
      {
        "name": "Windsurf (Devin Desktop) \u2014 v3.10.23 through v3.10.31",
        "status": "changed",
        "details": [
          "**Remote Agent Hosts over SSH (September 10, v3.10.23)** \u2014 connect to agents via SSH with **saved hosts**, status indicators and server icons in the sidebar; sessions recover exact transcripts after reconnect. **ACP is now always enabled** (the enable/disable toggle was removed). MCP authorization \"opens provider directly\" and **Docker-based MCP servers launch correctly**. Source: https://docs.devin.ai/desktop/changelog",
          "**Restricted Mode hardening (September 16, v3.10.31)** \u2014 \"Restricted Mode blocks restricted workspace settings written in **nested object form**, not just the dotted form.\" A fixed bypass in a workspace-trust control. Source: same"
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "Microsoft Foundry autopilots are agents that hold a real Entra *user* account \u2014 mailbox, calendar, OneDrive, Teams presence and a manager \u2014 and one blueprint fans out into an unbounded set of them",
        "description": "Foundry now documents a **third agent type** whose definition is identity, not capability. \"An autopilot is a type of agent that works under its own identity as a persistent, named member of your organization. It holds a standing role rather than answering one request at a time.\" Every Foundry agent already carries an Entra **agent identity** (a service principal). What makes an autopilot different is that it **also** carries an Entra **agent user account** \u2014 a *user object* \u2014 and Microsoft enumerates exactly what that buys: \"its own email, calendar, OneDrive, Teams presence, and a place in the organization chart,\" plus the ability to \"perform Microsoft 365 actions **as itself**.\" Microsoft states the test plainly: \"An agent either has its own agent user account or it doesn't, which is what makes identity a reliable definition. The distinction between an agent and an autopilot is **binary**.\" **This collapses the boundary Zenity's M365 and Entra collectors rely on.** An autopilot appears in the tenant's user object space, in the org chart, and in mail and Teams traffic as a named member of staff; it can act in **group chats** where no single user's permissions apply; and it \"can act without being prompted\" in response to events with **no user in the loop at all** \u2014 which is precisely the scenario Microsoft says motivated the feature. Any inventory, DLP rule, sharing-risk calculation or anomaly baseline that partitions principals into \"humans\" and \"service principals\" will now classify autopilots on the wrong side of the line. **The fan-out is the second half of the finding.** \"You don't build an autopilot. You build a blueprint that teams use to create their own autopilot instances, each with its own identity and its own team-scoped access.\" A regular Foundry agent is one-to-one \u2014 one agent, one blueprint, one agent identity, no user account. An autopilot blueprint is **one-to-many**: one blueprint, many *hired* instances, and **each instance gets its own agent identity *and* its own agent user account**. So a single reviewed, approved object silently becomes N mailbox-holding principals with N different access grants, each scoped by whichever manager hired it. Microsoft's own argument for the design is that it prevents ten teams from building ten near-identical agents whose \"controls drift apart,\" and that argument is sound \u2014 but the inventory consequence is that **the governed object and the acting principal are different objects at different cardinalities**, and only the blueprint is centrally reviewed. Three specifics are directly actionable. **First, the blueprint is the kill switch**: \"Update the blueprint and every instance updates. Block the blueprint and every instance stops.\" AISPM should model the blueprint\u2192instance relationship as a first-class inventory edge, because it is simultaneously the blast radius of a bad change and the only single point of containment. **Second, only Foundry *hosted* agents can be published as autopilot blueprints**, which ties this directly to the hosted-agent surface already tracked \u2014 including the **bring-your-own-registry** path reported on 2026-09-02, meaning an autopilot's container image can originate outside Azure entirely. **Third, Microsoft draws a tool/data distinction Zenity should adopt verbatim as a rule**: \"Holding a tool isn't the same as holding access to the data behind it. Giving an autopilot a SharePoint tool doesn't give it any SharePoint site, and giving it a mailbox doesn't give it the ability to send mail.\" Effective reach is therefore the **intersection** of the blueprint's tool grants and the instance's resource grants, and an inventory that reports only one of the two will systematically over- or under-state what an autopilot can do. AISPM should discover autopilot blueprints as an entity distinct from agents, enumerate hired instances per blueprint, resolve each instance's **agent user account** back to its Entra user object and its **manager**, join that user object against mailbox, OneDrive, Teams and SharePoint permission grants the same way it would for a human, and flag: an autopilot user account with no manager assigned; an instance whose resource grants exceed its sibling instances from the same blueprint; a **company-wide** autopilot (Microsoft's own pattern for \"one production instance per business process\") holding tenant-wide scope; and a **personal** autopilot created by a single user outside any review. AIDR should treat activity from an agent user account as agent activity, not human activity, wherever the two are currently conflated. Note the correction: these pages were published in the August Foundry roll-up and were in the material this monitor held back on 2026-09-02 without reading",
        "source": "https://learn.microsoft.com/en-us/azure/foundry/agents/concepts/autopilot-overview",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Anthropic moved per-call tool authorization onto its own server, added a decision record to every tool event, and documented the prompt-injection path into it",
        "description": "The new **`auto` permission policy** (September 10, `managed-agents-2026-04-01` beta) is a third option alongside `always_allow` and `always_ask`, and it changes where the authorization decision happens: \"the server evaluates each call before it runs. Because the evaluation considers the tool, the call's input, and the session's content up to that point, **the server can treat two calls to the same tool differently**.\" Three outcomes \u2014 run, **deny**, or pause for approval \u2014 and the denial is final: the agent receives `Permission to use {tool_name} has been denied.` with `is_error: true`, and \"**your client cannot override the denial**.\" Sending a `user.tool_confirmation` for a server-denied call is rejected with a 400. **Three things follow for Zenity.** **First, there is now a decision record on every tool call, under every policy.** `agent.tool_use` and `agent.mcp_tool_use` events carry **`evaluated_permission`** (`\"allow\"` / `\"ask\"` / `\"deny\"`) and, in most cases, an **`evaluation`** object naming the resolving policy; under `auto` it nests the server's determination and a **`reason_code`** of `high_risk` or `indeterminate`. This is a genuinely new, free, first-party detection signal \u2014 **a `deny` with `reason_code: high_risk` is Anthropic telling you an agent tried something its own classifier judged dangerous**, and AIDR should ingest it as a first-class alert rather than as event metadata. Two absence cases must be handled correctly or they will read as gaps: `evaluation` is missing when the agent names a tool that isn't enabled (the server denies without evaluating, so `evaluated_permission: \"deny\"` with no `evaluation` means *unknown tool*, not *blocked by policy*), and it is missing on events predating the field, which Anthropic says to read as `always_allow` when the outcome is `allow` and `always_ask` when it is `ask`. `agent.custom_tool_use` carries neither field, so **custom tools remain entirely client-authorized and produce no decision record at all** \u2014 that is the blind spot in this telemetry and it is where an adversary would put the tool that matters. **Second, `auto` is an injection target and Anthropic says so.** \"What you post in `user.message` events counts as your intent, and it can lead the server to allow a call it would otherwise deny.\" The server \"assesses but does not take instructions from\" tool results, fetched webpages, MCP responses, or messages between session threads \u2014 but `user.message` is trusted, and: \"**If you relay untrusted end-user input in `user.message` events, the server reads that input as your intent too, and it can get a call allowed.**\" That is an explicit, vendor-documented privilege-escalation path in any application that forwards end-user chat into a Managed Agents session, which is the overwhelmingly common architecture. **AIRT should build this as a probe**: place attacker-controlled text in `user.message`, attempt a tool call the server denies without it, and measure whether the denial flips. **Third, `auto` will be mistaken for a control it is not.** Anthropic's own warning: \"`auto` is not a human checkpoint. If the server determines that a call is safe, the call runs before anyone sees it, and its effects might not be reversible.\" AISPM should inventory the resolved permission policy **per toolset and per tool** (defaults are agent toolset `always_allow`, MCP toolsets `always_ask`, and no toolset uses `auto` by default), flag any agent that **downgraded an MCP toolset from `always_ask` to `always_allow` or `auto`**, flag `auto` on destructive tools such as `bash`, and record that **running sessions keep the toolset configuration they were created with** \u2014 so a policy tightening does not take effect on sessions already in flight, and a point-in-time posture read will disagree with live session behavior",
        "source": "https://platform.claude.com/docs/en/managed-agents/permission-policies",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "`ant apply` turns Claude agent fleets into Git-managed infrastructure \u2014 including skills pulled straight from GitHub URLs \u2014 and the authoritative inventory is now a lockfile in a repository",
        "description": "`ant apply` (CLI \u2265 1.30.0, September 3) \"creates and updates Claude API resources from files: **agents, environments, skills, memory stores, and deployments**. They live in your repository and change through the same review as your code.\" This is the Claude platform's answer to Terraform, and it relocates the source of truth for an agent fleet **out of the Anthropic Console and into version control**. Five properties matter. **First, the resource set is the whole attack surface.** An agent file's frontmatter is its configuration and its body is its **system prompt**; an **environment** YAML declares the sandbox including `networking: {type: unrestricted}`; a **memory store** YAML declares persistent cross-session state; a **deployment** Markdown carries a **cron schedule** and prose that \"becomes the message that starts each session\" \u2014 i.e. **an unattended, recurring agent trigger defined in a text file**. Every one of these is an entity Zenity already models, and all of them now change by pull request. **Second, skills can be pulled from arbitrary GitHub URLs.** A skill reference may be `https://github.com/<owner>/<repo>/tree/<branch>/<dir>`, which `ant apply` \"downloads and uploads,\" pinned to the resolved commit **until you run with `--upgrade`** (with `GITHUB_TOKEN` for private repositories). That is a third-party supply-chain edge into an agent's capability set, expressed as one line of YAML, and the pinning is only as good as the discipline around `--upgrade`. This is the [[agent-plugins-cross-vendor-standard]] pattern again: capability arriving from a repository rather than from a platform control plane. **Third, `claude-lock.json` is the inventory artifact and it is committed to the repo.** It records, per file, the resource `kind`, its API `id`, its `version`, and **two hashes** \u2014 `hash` (what was last sent) and `remote_hash` (what the API returned) \u2014 plus the bound `organization_id` and `workspace_id`. Zenity should read this file directly: it is a complete, machine-readable manifest of an organization's declared agents, and the hash pair is a **drift detector** \u2014 a mismatch means a resource was changed outside the files, which `ant apply` refuses to overwrite without `--force`. AISPM should treat the presence of `--force` in a CI pipeline as a finding, because it means declared state is being pushed over out-of-band changes without review. **Fourth, CI runs under Workload Identity Federation.** Anthropic recommends `ant apply --yes .` on the default branch after merge, authenticated by WIF rather than a stored key, and the command \"refuses credentials that resolve to any other organization or workspace.\" Good design \u2014 and it means **a CI identity now holds create-and-update rights over an organization's entire agent fleet**, which AISPM should enumerate as a privileged non-human principal alongside the personal and service-account keys reported on 2026-09-02. **Fifth, the adoption gap is real and detectable**: `ant apply` \"can't adopt a resource you created in the Console or with `ant beta:agents create`,\" and applying a file describing an existing agent **creates a second one**. So organizations mid-migration will have duplicate agents, and only the lockfile-tracked half is governed by code review. AISPM should discover `claude-lock.json` files in customer repositories, parse them into agent/environment/memory-store/deployment inventory, reconcile that list against the Admin API's live resource list in **both directions** (lockfile-but-not-live = pruned or stale; live-but-not-lockfile = Console-created and outside code review), flag deployments carrying cron schedules as unattended triggers, flag environments with unrestricted networking, resolve every GitHub-URL skill reference to its pinned commit and the repository's ownership, and alert when `--prune` would archive a resource",
        "source": "https://platform.claude.com/docs/en/cli-sdks-libraries/cli/apply",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "ServiceNow Zurich is GA with a Build Agent that writes apps into the global scope and an AI Control Tower that publishes managed agents to external registries",
        "description": "The Zurich release family reached GA (highlights page updated **2026-08-07**) and it is the most agent-dense ServiceNow release this monitor has seen. Four items, each a distinct coverage gap. **First, \"publishing managed agents to external registries\" \u2014 zero hits across every prior report.** This is the outbound complement to two edges already recorded: the ServiceNow\u2192**Microsoft Agent 365** publication path found on 2026-09-02, and Agent 365's own auto-ingest of Bedrock and Vertex agents found on 2026-08-30. ServiceNow is now a **general-purpose agent publisher** into other vendors' registries, which means a ServiceNow-authored agent can appear in a customer's AWS, Microsoft or Google agent inventory as a native record while its logic, its tool grants and its lifecycle remain in ServiceNow. AISPM must model registry publication as an inventory **edge** and must not count the source and the published copy as two agents; more importantly it must determine **which platform is authoritative** for the agent's permissions, because the registry record will show the destination platform's view and the destination platform cannot revoke what ServiceNow granted. **Second, Build Agent creates applications in the *global scope*.** Build Agent generates ServiceNow applications from natural-language prompts inside ServiceNow Studio, and Zurich adds \"the ability to create apps and newly supported metadata **in the global scope**\" plus \"additional **Model Context Protocol (MCP)** support.\" Scoped applications are ServiceNow's isolation primitive; **global scope is the absence of it**. An LLM-driven app generator that can write global-scope metadata is a privilege-escalation surface inside the Now Platform itself, and the MCP support means that generator can reach external tools while doing it. AIRT should treat natural-language app generation into global scope as a prompt-injection target with platform-wide consequences, and AISPM should inventory Build Agent-generated applications by scope, flagging every global-scope artifact. **Third, Machine Identity Access Controls** \u2014 \"Enforce access to data via REST or SOAP endpoints\" \u2014 is ServiceNow shipping first-party non-human-identity authorization, which is both an ingestion source (it is a permission model Zenity can read) and a competitive overlap. **Fourth, the redesigned AI Control Tower** adds **automated deduplication** of redundant AI assets, **AI Service Graph Connectors** discovering assets across AWS, Microsoft and GCP, and **kill switch** containment for rogue agents. The dedup logic is the one to watch: an inventory that silently merges records it believes are duplicates will disagree with Zenity's counts, and the merge criteria are undocumented. AISPM should ingest AI Control Tower's asset graph where a customer runs it, reconcile it against Zenity's own discovery, and treat a Control Tower dedup as a hypothesis to verify rather than a fact. **Source caveat, unchanged for five scans**: the family index at `servicenow.com/docs/r/release-notes/family-release-notes.html` \u2014 the URL in `sources.json` \u2014 asserts no feature content; the productive page is the Zurich highlights roll-up",
        "source": "https://www.servicenow.com/docs/r/zurich/release-notes/rn-summary-highlights.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "AgentCore added an AWS-hosted consent portal \u2014 a new delegation surface where end users grant agents access to their resources, with its own resource type and CRUD API",
        "description": "The **Consent Portal** (September) is \"a hosted, AWS-managed portal that authenticates your end users to an OpenID Connect (OIDC) identity provider (IdP) and **gathers their consent before your agent accesses a downstream resource on their behalf**.\" It is a new AgentCore Identity resource with **create, get, list, update and delete consent-portal operations**, a dedicated **consent portal execution role**, and configurable **consent portal targets**. Each portal **attaches to exactly one AgentCore Gateway** as its source, requires that gateway to use **JWT inbound authentication**, and uses an **OAuth2 credential provider** referencing the same IdP the gateway's authorizer trusts, whose permitted scopes must include `openid`. Users are directed to the portal's **`portalUrl`**. AWS notes the flow stays server-side: \"the browser never holds a token.\" **Why this matters more than a typical identity feature.** Delegated consent is the mechanism by which an agent acquires a human's authority over a third-party system, and until now AgentCore's consent path was something each builder assembled themselves \u2014 meaning it was idiosyncratic, hard to find, and effectively invisible to inventory. A **hosted, named, enumerable resource** changes that in both directions: it gives Zenity an API to enumerate (**which agents can ask which users for which scopes**), and it gives an attacker a **credible, AWS-branded, AWS-hosted URL** to phish with. A consent portal is a consent-grant UI hosted on AWS infrastructure, presented to end users, that results in an autonomous agent holding standing access to their resources \u2014 the classic OAuth illicit-consent-grant attack, with the added property that the relying party is a non-human that will use the grant unattended. AISPM should add **consent portals as an inventory entity**, resolve each portal to its **source Gateway** and to the **OAuth2 credential provider and IdP** behind it, enumerate the **scopes** a portal can request and flag any portal requesting scopes broader than its agent's documented purpose, and treat the combination of a consent portal plus a gateway with permissive JWT `AllowedClients`/`AllowedAudience` as a toxic pair \u2014 noting that those exact authorizer fields are the ones AWS Agent Registry **does not carry into its records** (recorded 2026-09-02), so a registry-derived view cannot evaluate this pairing and Zenity's must read the gateway directly. AIDR should model consent grants and revocations through a portal as activity, since a grant is the moment an agent's effective reach expands and there is otherwise no tool-configuration change to observe. AIRT should probe whether a portal's `portalUrl` can be reached and presented out of context, and whether the consent screen names the agent in a way an end user could verify",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/identity-consent-portal.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "The Claude Compliance API now returns Claude in Chrome session transcripts \u2014 a browser-resident agent surface Zenity has never collected",
        "description": "As of **September 18**, the Compliance API's **local** session endpoints return transcripts of **Claude in Chrome** sessions under **`product_surface: claude_in_chrome`**, in beta for Claude Enterprise organizations. The covered surface list is now **Cowork, Claude Code, Claude Science, Claude for Microsoft 365, and Claude in Chrome** \u2014 with Cowork and Claude Code stable and the other three in beta. No new key, scope, setting or client update is required: the existing **Compliance Access Key** with **`read:compliance_user_data`** works, so any Zenity deployment already collecting Claude Code and Cowork transcripts can extend to this surface with a parameter change and no customer action. **The significance is the surface, not the endpoint.** A browser-resident Claude agent operates inside the user's authenticated web sessions \u2014 every SaaS application the user is logged into, with the user's cookies, in the user's tab. Its transcript is therefore a record of agent activity against systems that have **no server-side agent integration at all** and would appear in those systems' logs as ordinary user activity. This is the closest thing available to ground truth for browser-agent behavior, and it is the natural counterpart to the Claude for M365 office-agents surface recorded in [[claude-office-agents-coverage-gap]]. AIDR should add `claude_in_chrome` to the local-session collection set, model browser-agent tool calls as a distinct activity type (the transcript is \"the sequence of user prompts, assistant responses, and **tool calls and results**\"), and correlate browser-agent sessions against the same user's activity in monitored SaaS applications to identify actions that were agent-driven rather than human. AISPM should record which of the five product surfaces a customer's Claude Enterprise organization actually has in use, because each one is a separate endpoint surface with a separate stability tier \u2014 and the three beta surfaces are exactly the three newest, meaning collection coverage lags product adoption by design. The documented blind spots recorded in [[anthropic-source-and-coverage-gaps]] continue to double as a coverage map and should be re-read against this expanded surface list",
        "source": "https://platform.claude.com/docs/en/manage-claude/compliance-sessions",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Gemini Enterprise Projects is a new user-created knowledge-base entity that blends Drive, SharePoint and local uploads under per-project membership",
        "description": "**Projects** (September 4, GA) lets any user \"build a dedicated knowledge base for your own work or to collaborate with your team,\" holding up to **50 files and linked documents** drawn from local uploads (PDF, DOCX, PPTX, XLSX, CSV, HTML, TXT), **Google Drive files and folders**, free-text notes, and **third-party connectors such as Microsoft SharePoint**. This is a new inventory entity with its own membership model, and three of its properties are posture-relevant. **First, the permission model is mixed.** Linking Drive content \"doesn't change their original sharing permissions\" and members \"only see files they have permission to access\" \u2014 correct, permission-trimmed behavior. But **locally uploaded files are visible and deletable by all members**, which means a project is simultaneously a permission-respecting view over Drive *and* a permission-flattening store for anything uploaded directly. A single object with two different access semantics is exactly where data leaks, and an inventory that records a project's member list without distinguishing linked from uploaded content will misstate exposure. **Second, invited members receive Editor permissions by default** \u2014 the default grant is write, not read. **Third, projects are user-created**, so this is shadow-knowledge-base creation as a product feature: a user can assemble corporate SharePoint content, Drive folders and uploaded documents into a durable, shared, agent-groundable corpus with no administrative involvement. AISPM should inventory Gemini Enterprise projects as a data-store entity, record per-project membership and owner, **separate linked content (permission-inherited) from uploaded content (project-flattened)** in exposure calculations, and flag projects containing uploaded files alongside members who would not have had access to the equivalent source documents. Two companion items from the same window: **Google Cloud Marketplace now supports semantic search for agent discovery** (September 8), lowering the friction for a user to find and adopt a third-party agent \u2014 a shadow-agent acquisition path worth watching; and **nine new preview connectors landed in three weeks** (CoinDesk, Relativity, Streak, Superhuman Mail on September 4; Campfire, Clay, CourtListener, Daloopa, MSCI on August 28) with **new actions** for AirOps, Airtable and Zoho Desk, continuing the read-to-write expansion pattern reported on 2026-08-21, 2026-08-30 and 2026-09-02 where Google widens what already-connected data stores can *do* without the connector list changing. Sources: https://docs.cloud.google.com/gemini/enterprise/docs/projects and https://docs.cloud.google.com/gemini/enterprise/docs/release-notes",
        "source": "",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Salesforce shipped Koa, a CRM-specific reasoning model trained for tool use, and repositioned the platform as AIforce",
        "description": "At Dreamforce (September 15-17) Salesforce announced **Koa**, \"Salesforce's first CRM reasoning model,\" built by post-training **NVIDIA Nemotron 3 Super** on a proprietary synthetic dataset \"modeled on enterprise knowledge from nearly three decades of CRM deployments.\" Salesforce's own framing is the part that matters to Zenity: Koa is \"purpose-built to help agents **reason through complex, multistep workflows and use the right tools** to get work done,\" and its training corpus reflects \"the reasoning, **tool use**, and decision-making skills Agentforce agents perform across CRM workflows.\" No customer data was used in training. **Availability is select pilot customers now in Agentforce, GA expected winter 2026 in U.S. regions.** Three consequences. **First, a new model identifier enters the Agentforce inventory** \u2014 AISPM should add Koa to the model watchlist and record which Agentforce agents are configured to use it, because a pilot-gated model in a customer tenant is evidence of an early-access arrangement the customer's own governance may not have reviewed. **Second, a model explicitly optimized for tool selection changes AIRT's assumptions.** Red-team fixtures calibrated against general-purpose models will not predict the behavior of a model post-trained specifically on CRM tool-calling: it will be more willing to chain tool calls, and more confident in selecting them, which raises the value of prompt-injection testing against Agentforce agents rather than lowering it. AIRT should build Koa-specific probes once GA lands and should not assume Agentforce guardrail behavior is model-independent \u2014 the lesson from [[claude-mythos-5-safeguards-lifted]] is that guardrail behavior is a property of the deployment. **Third, `AIforce` appears to be a platform-level rebrand** (announced the same day) of the kind that historically breaks Zenity's name-matching in rules, reports and connector labels \u2014 the same failure mode as the Vertex\u2192Gemini Enterprise rename in [[vertex-now-gemini-enterprise]] and the Foundry RBAC role rename reported on 2026-09-02. Salesforce documentation had not caught up at scan time: `developer.salesforce.com/docs/ai/agentforce/guide` returns **403** and `salesforce.com/releases/` remains unreachable, so both the model's configuration surface and the rebrand's scope need a follow-up read once the Winter '27 docs publish. Sources: https://www.salesforce.com/news/press-releases/2026/09/15/koa-reasoning-model/ and https://www.salesforce.com/news/",
        "source": "",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Cursor added a coordinator agent that fans out to \"thousands of subagents\" and self-hosted machines that keep agent tool execution inside the customer's own network",
        "description": "Two September releases that together move Cursor from an IDE with an agent into an agent platform with an IDE. **Cursor Projects (September 10)** introduces a **coordinator agent** that \"delegates tasks to thousands of subagents in parallel,\" maintains **shared context across cloud and local machines**, and supports **subscriptions** that monitor Slack channels and run scheduled tasks. Three distinct problems for ENDPOINT. The *cardinality* \u2014 an inventory that counts agents per workstation is meaningless against a coordinator that spawns subagents at that scale; the unit of inventory has to become the coordinator and its delegation policy. The *context bridge* \u2014 state deliberately spanning cloud and local means a local capture is a partial view by design, and data can traverse the boundary without a file ever landing on disk. And the *triggers* \u2014 a subscription watching a Slack channel is an **unattended, externally-triggered agent whose input is attacker-reachable**: anyone who can post in a monitored channel can initiate agent work. AIRT should treat Slack-channel subscriptions as a first-class prompt-injection entry point. **Self-Hosted Machines (September 2)** is the complement: infrastructure for \"keeping tool execution within internal networks,\" with dynamic pool scheduling and autoscaling, **computer use on Linux and Mac with desktop automation**, and integrations spanning AWS Lambda, Coder, Cloudflare, Daytona, Modal, Namespace, Vercel and E2B. Read from a defender's seat this is welcome \u2014 execution moves inside the perimeter where it can be observed \u2014 but it also means **Cursor agents now run as workloads in the customer's own cloud accounts**, with desktop automation, reachable from a vendor control plane. ENDPOINT and AISPM have to cover this jointly: these are neither endpoint processes nor cloud agents but both. AISPM should discover self-hosted machine pools as agent compute resources, attribute them to the Cursor organization that schedules them, and enumerate the credentials they carry; ENDPOINT should treat a workstation participating in a Project's shared context as holding data whose origin is outside it. Cursor remains **watching** tier, but the last three scans have produced Cursor findings and this pair is a capability jump, not an increment \u2014 a tier review is warranted",
        "source": "https://cursor.com/changelog",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Anthropic's compact-on-demand returns a signed summary block that replaces conversation history the API will never show again",
        "description": "**Compact-on-demand** (beta, September 14, `compact-2026-09-04` header) adds a top-level **`compaction`** parameter to the Messages API. The API returns a **signed `compaction` block** summarizing prior messages and \"can keep recent turns word-for-word while preserving thinking validity.\" Operationally this is a context-window management feature and a good one. For monitoring it is **evidence compression at the API boundary**: the turns folded into a compaction block are replaced by a model-authored summary, and anything an investigator would have read in the original \u2014 an injected instruction, a tool result, a leaked value, the exact phrasing that preceded a harmful action \u2014 survives only insofar as the summarizer chose to carry it. A signed summary is not a transcript. This compounds the **per-tool MCP `output_token_limit`** truncation setting reported on 2026-09-02: the pipeline now has two independent, configurable points at which content is discarded before it reaches anything durable. AIDR should treat the presence of a `compaction` block as a **collection-integrity signal**, flag sessions where compaction occurred so downstream analysis is not read as complete, and \u2014 where the Compliance API is the collection path \u2014 verify empirically whether compacted turns remain retrievable in exported transcripts or whether the export reflects the compacted view. That question determines whether compaction is a cosmetic concern or a genuine eDiscovery gap, and it should be answered before a customer asks. The block being **signed** is the constructive half: a signature implies verifiable provenance, so AIDR may be able to detect a *forged* or *tampered* compaction block, which is worth confirming with Anthropic",
        "source": "https://platform.claude.com/docs/en/build-with-claude/compaction",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Codex CLI added a Windows background daemon with scheduled self-updates, isolated git worktrees, and biometric gating on MCP requests",
        "description": "Two releases in nine days materially change what Codex is on a workstation. **0.154.0 (September 9)** added a **Windows background server with daemon lifecycle commands** and experimental **worktree support** for isolated checkouts in new and forked sessions; **0.155.0 (September 17)** added **configurable daemon update schedules with thread recovery after restarts**, **Touch ID verification for MCP requests** on supported Macs, **AWS credential sourcing from configured commands for Amazon Bedrock**, and experimental **voice conversations**. The composite is a **persistent, self-updating background process on Windows and macOS that survives restarts, recovers its agent threads, and holds cloud credentials** \u2014 which is a different endpoint object than a CLI a developer invokes. ENDPOINT should discover the Codex daemon as a running service rather than as a foreground process, record its **configured update schedule** (a self-update channel is a supply-chain path, and one whose cadence is now customer-configurable and therefore auditable), and capture thread-recovery state as agent state that persists across reboots. **Worktrees** are the second item: an agent operating in an isolated checkout is working on files that are not the developer's working tree, so a monitor watching the primary checkout sees nothing \u2014 ENDPOINT should enumerate git worktrees as agent-reachable filesystem scope. **`AWS credential sourcing from configured commands for Amazon Bedrock`** means Codex now obtains AWS credentials by **executing an arbitrary configured command**, which is both a credential-acquisition path to inventory and a code-execution vector defined in local configuration. The constructive item is **Touch ID verification for MCP requests**: a biometric, per-request human checkpoint in front of MCP tool calls is the strongest local control Codex has shipped, and its *absence* on a machine that supports it is now a detectable posture gap \u2014 ENDPOINT should assess whether it is enabled. Also noted: **GPT-6-Astra** is now selectable in Codex and **in Amazon Bedrock catalogs**, a new model identifier for the model-inventory watchlist across both OpenAI and AWS paths",
        "source": "https://learn.chatgpt.com/docs/changelog",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Devin Desktop can now drive agents on remote hosts over SSH, always runs ACP, and patched a workspace-trust bypass",
        "description": "**v3.10.23 (September 10)** added **Remote Agent Hosts**: connect to agents via **SSH with saved hosts**, status indicators and server icons in the sidebar, with sessions that \"recover exact transcripts after reconnect.\" The desktop application is now a **control console for agents executing on other machines** \u2014 the agent's filesystem, network position and credentials are the remote host's, not the workstation's, so an endpoint agent inspecting the local machine will find a UI and a transcript but no execution. ENDPOINT should enumerate **saved SSH hosts** as agent execution targets and treat each as a separate scope requiring its own coverage. The same release made **ACP always enabled** by removing the enable/disable toggle \u2014 a protocol surface that was previously optional is now unconditional and should be assumed present on every install \u2014 and changed MCP behavior so that **authorization \"opens provider directly\"** and **Docker-based MCP servers launch correctly**, meaning containerized MCP servers are now a working and therefore likely-to-appear configuration on these endpoints. Separately, **v3.10.31 (September 16)** fixed a real bypass worth recording: \"Restricted Mode blocks restricted workspace settings written in **nested object form**, not just the dotted form.\" Restricted Mode is the workspace-trust boundary, and until this release a restricted setting expressed as a nested object rather than a dotted key **was not blocked** \u2014 a straightforward serialization-level bypass of a trust control. ENDPOINT should confirm customers are on \u2265 v3.10.31 and should generalize the lesson: workspace-trust controls that match on key *syntax* rather than resolved *configuration* are bypassable by re-encoding, and the same test is worth running against the equivalent controls in Cursor, Kiro and VS Code. Note the source drift confirmed again this scan: `windsurf.com/changelog` 308-redirects to `docs.devin.ai/desktop/changelog`",
        "source": "https://docs.devin.ai/desktop/changelog",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      }
    ]
  }
]