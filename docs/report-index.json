[
  {
    "date": "2026-08-23",
    "scan_time": "2026-08-23 08:43",
    "file": "report-2026-08-23.md",
    "platforms": [
      {
        "name": "Amazon Bedrock AgentCore \u2014 one new August entry (12th; was 11 on 2026-08-21)",
        "status": "changed",
        "details": [
          "**Memory: store and extract non-conversational JSON payloads.** `CreateEvent` now accepts a **`json` payload type**, letting agents store non-conversational, JSON-formatted data \u2014 \"behavioral events, activity logs, and system events\" \u2014 at **up to 100 KB for each payload**. The API reference confirms the extraction asymmetry: \"**Conversational and JSON payloads are extracted into long-term memory. Blob payloads are stored in short-term memory only and are not extracted.**\" JSON payloads are processed by the **semantic, user preference, summarization and episodic** strategies. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/short-term-create-event.html",
          "*(Verified no other movement: the AgentCore data-plane Actions list is **operation-for-operation identical** to the 2026-08-07 capture \u2014 no net-new API operations.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Claude Enterprise (Anthropic) \u2014 watch item closed; one new dated entry held below the bar",
        "status": "error",
        "details": [
          "**`claude-mythos-5` / Project Glasswing \u2014 the 2026-08-21 open watch item, now resolved.** Anthropic's Mythos model page states it plainly: users with Mythos Preview access can upgrade to Claude Mythos 5, \"**the same model as Claude Fable 5 but with cyber safeguards lifted**.\" The mechanism is classifier removal \u2014 **Fable 5 runs three real-time safety classifiers that route sensitive queries, and on Mythos 5 those classifiers are absent**, which is what permits unrestricted offensive security analysis for defensive work. Mythos 5 \"is currently only provided to a small, but growing, set of customers through trusted access programs,\" is \"offered only to approved customers in Project Glasswing,\" and the access route is to \"contact your **Anthropic, AWS, or Google Cloud account team**\" \u2014 three clouds, not one. Glasswing's launch partners are **AWS, Apple, Broadcom, Cisco, CrowdStrike, Google, JPMorganChase, the Linux Foundation, Microsoft, NVIDIA and Palo Alto Networks**; the Mythos 5 rollout expanded the programme to roughly **150 organizations across more than 15 countries**, and access additionally requires an Anthropic invitation, a signed NDA, and approval coordinated with the US government. Recovered via WebSearch \u2014 `anthropic.com/news` carries nothing after Aug 14 and does not mention Mythos or Glasswing at all. Source: https://www.anthropic.com/claude/mythos",
          "*(Not reported: **Python SDK v1.0**, Aug 20 \u2014 the HTTP layer moves from `httpx` to `httpx2`, Python 3.10+ is required, and the release **removes** the legacy Text Completions API, the `temperature`/`top_p`/`top_k` parameters on Messages methods, and the tool runner's client-side `compaction_control`; `AnthropicBedrock` now errors when no AWS region is set instead of defaulting to `us-east-1`. A version bump plus deprecations.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Microsoft Foundry \u2014 no new release content; one never-read API surface",
        "status": "changed",
        "details": [
          "**The Foundry Skills API (`{endpoint}/skills`) \u2014 preview.** A skill is a `SKILL.md` file following the [agentskills.io](https://agentskills.io) specification (YAML front matter `name` + `description`, Markdown body that \"becomes the skill's injected instructions\"), stored centrally through a **versioned API**. Creating a version auto-creates the parent skill; **every update creates a new immutable `SkillVersion`**, and the parent `Skill` tracks `default_version` and `latest_version`. Full CRUD plus content download as a ZIP: `POST|GET|DELETE {endpoint}/skills[/{name}[/versions[/{v}]]]`, `GET .../content`. All calls require the **`Foundry-Features: Skills=V1Preview`** header and a bearer token scoped `https://ai.azure.com/.default`; RBAC is **Foundry User** on the project. Two delivery modes: **attach to a toolbox**, where skills surface as **MCP Resources** under the SEP-2640 Skills extension so any client calling `resources/list`/`resources/read` \u2014 \"GitHub Copilot, Claude Code, or your own agent harness \u2026 without any Foundry SDK\" \u2014 discovers and loads them; or **download directly** into a hosted or local agent project for per-session context injection. The VS Code Toolkit ships a **prebuilt skill catalog** (`docx`, `pptx`, `xlsx`, `pdf`, `canvas-design`, `algorithmic-art`, `brand-guidelines`, `theme-factory`, `doc-coauthoring`, `internal-comms`, `slack-gif-creator`). Limitations worth recording: **the Skills API is not reachable over a private endpoint**, so a Foundry resource with public network access disabled cannot use skills at all; and `azd ai skill create --force` **deletes the existing skill and all of its versions** before uploading. Microsoft's own caution: \"Customers are responsible for understanding the behaviors of any skills deployed and the data handling practices of any external endpoints receiving data.\" Source: https://learn.microsoft.com/en-us/azure/foundry/agents/how-to/tools/skills",
          "*(Recorded without a finding: the `reminder_preview` self-scheduling tool \u2014 reported 2026-08-09 inside the Routines finding \u2014 now has its own page with concrete bounds: `minutes` accepts **1 to 43,200** (30 days), `input` carries instructions for the re-invocation, the tool is **connectionless** (no external service or authentication), it is **hosted-agents-only**, and Foundry creates a **scheduled routine** that re-invokes the same agent **on the same conversation**, preserving context \u2014 unlike regular routines, which start new ones. Also noted: the Foundry RBAC roles were renamed \u2014 **Azure AI User/Owner/Account Owner/Project Manager \u2192 Foundry User/Owner/Account Owner/Project Manager**, with role IDs and permissions unchanged.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "ChatGPT Enterprise (OpenAI) \u2014 no new changelog entries; four unread SDK releases",
        "status": "changed",
        "details": [
          "**v3.2.0 (Aug 17) \u2014 \"add Bedrock Runtime endpoint support (SDK-290)\"** and \"Add shell call streaming events and new service/image types\". Source: https://github.com/openai/openai-python/releases/tag/v3.2.0",
          "**v3.3.0 (Aug 18)** \u2014 \"support named data-residency endpoints\". **v3.3.1 (Aug 19)** \u2014 security fixes and build tooling only. **v3.1.0 (Aug 14)** \u2014 \"add WebSocket stream IDs\", \"add workload identity access token issued event\", and Sora video APIs marked deprecated. Source: https://github.com/openai/openai-python/releases",
          "*(`gpt-realtime-2.1` / `-mini` appears in none of the four \u2014 seventh consecutive scan absent.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Windsurf / Devin Desktop (watching tier) \u2014 v3.8.20, August 21",
        "status": "changed",
        "details": [
          "**Terminal and user-edit activity can be shared with ACP agents, and it is now a setting.** v3.8.20 adds settings that \"control whether **terminal and user-edit activity share with ACP agents**.\" Following this to the ACP docs establishes the receiving side: **ACP is JSON-RPC 2.0 over stdin/stdout and the editor launches the agent as a subprocess**; Devin Desktop supports **Codex, Claude Agent, OpenCode and any other ACP-compatible agent, \"including agents built by your team in-house\"**; third-party agents appear in the Kanban view **alongside** Devin, **share the Spaces context layer**, and \"operate with the same controls as native sessions.\" Available on Pro, Max and Teams. Also in v3.8.20: **workspace trust prompts now appear when needed to activate local agents**, and **multi-root workspaces with virtual filesystem folders**. Source: https://docs.devin.ai/desktop/acp",
          "*(Not reported: the remaining v3.8.20 items \u2014 Agent Command Center workspace adaptation and separate-window setting, Windows restart-confirmation and CLI co-update, Linux keyring preservation, session-tab renaming, ACU-limit notices, plan-mode Markdown, stopping individual subagents, migration wizard over remote SSH, and the Devin Cloud sidebar/live-shell/copy-URL work.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Salesforce AgentForce \u2014 Winter '27 detail firmed up; one source recovered",
        "status": "changed",
        "details": [
          "**The second Winter '27 marketing agent is named: Agentforce Content Agent**, which \"helps marketers create campaign content at scale while maintaining brand consistency.\" Last scan could name only the first of the two. Source: https://help.salesforce.com/s/articleView?id=release-notes.rn_agentforce.htm&release=262&type=5",
          "**The Marketing Goals Agent operates autonomously against a budget.** Users \"define a goal, budget, and eligible audience, after which the agent generates a recommended strategy for review and approval. Once activated, it operates **autonomously within those guardrails, continuously optimizing** activities and decisions toward the desired outcome.\" Source: https://help.salesforce.com/s/articleView?id=release-notes.rn_agentforce.htm&release=262&type=5",
          "**Agentforce observability ships with refined agent analytics and custom scorers**, some features in Beta, alongside agent interoperability, voice, multilingual conversations and improved agent management. Source: https://help.salesforce.com/s/articleView?id=release-notes.rn_agentforce.htm&release=262&type=5"
        ],
        "tier": "primary"
      },
      {
        "name": "Amazon Q \u2014 dead source, and a named successor that is unwatched",
        "status": "changed",
        "details": [
          "**AWS will discontinue Amazon Q Developer IDE plugins on April 30, 2027** and directs the entire population to **Kiro** for \"the latest models and features, including agentic coding, chat and MCP support,\" stating that \"everything you rely on today (inline suggestions, chat, and code generation) is available in Kiro.\" The deprecation itself is not reportable; the **named migration target is**, because Kiro is absent from `sources.json` and has never been scanned by this monitor. Source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/q-developer-ide-end-of-support.html"
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "Claude Mythos 5 is Fable 5 with its cyber safeguards lifted, entitled per-organization through Anthropic, AWS *or* Google Cloud",
        "description": "The watch item opened on 2026-08-21 resolves into the most consequential finding of this scan, and Anthropic states the core fact directly: Mythos 5 is \"**the same model as Claude Fable 5 but with cyber safeguards lifted**.\" The mechanism is **classifier removal** \u2014 Fable 5 runs three real-time safety classifiers that route sensitive queries, and on Mythos 5 those classifiers are absent, which is precisely what permits unrestricted offensive security analysis for legitimate defensive work. Three consequences follow, and the first strikes at an assumption inside Zenity's own product. **Guardrail behaviour is a property of the deployment, not of the model.** Any AIRT baseline, any AIDR detection tuned to \"what this model refuses,\" and any posture rule that infers safety posture from a model family is wrong for this tier \u2014 two entitlements to the *same weights* behave differently, and nothing but the model identifier `claude-mythos-5` distinguishes them. Refusal-based guardrail probes will silently report a pass on a model that has no guardrail to bypass. **Access is a gated, auditable, multi-cloud entitlement.** Mythos 5 \"is offered only to approved customers in Project Glasswing,\" requires an Anthropic invitation, a **signed NDA**, and approval coordinated with the **US government** \u2014 and, critically for discovery, the documented request path is to \"contact your **Anthropic, AWS, or Google Cloud account team**.\" So the entitlement can arrive through a hyperscaler relationship rather than a direct Anthropic contract, meaning an organization may hold classifier-free model access that never appears in its Anthropic tenant at all. Zenity must look for it on all three surfaces. The population is not hypothetical: Glasswing's launch partners are **AWS, Apple, Broadcom, Cisco, CrowdStrike, Google, JPMorganChase, the Linux Foundation, Microsoft, NVIDIA and Palo Alto Networks**, and the Mythos 5 rollout expanded the programme to roughly **150 organizations across more than 15 countries** \u2014 a meaningful share of large enterprises, several of them likely Zenity customers. **And it is the direct structural analogue of OpenAI's Daybreak Red / GPT-5.6-Cyber tier** reported 2026-08-14, where OpenAI's own evaluation showed **95%** compliance on advanced offensive prompts versus **1.5%** for the default-protected model. This is now a **cross-vendor pattern**, so Zenity should model \"safeguard-lifted / cyber-capable model entitlement\" once as an entity type and map it per vendor rather than treating each programme as bespoke. AIRT should build a distinct baseline for safeguard-lifted tiers and stop treating refusal as a signal on them. AISPM should discover which models an organization is actually entitled to across Anthropic, Bedrock and Vertex/Gemini, treat `claude-mythos-5` and the GPT-5.x-Cyber tiers as privileged entitlements requiring named ownership and written justification, and flag any agent, API key or workspace configured against one. Note the discovery hazard that caused this to be missed for months: **Mythos 5 appears in ordinary model-availability lists alongside Fable 5, Opus 5 and Sonnet 5**, so it reads as one more model rather than as a control-stripped tier",
        "source": "https://www.anthropic.com/claude/mythos",
        "module": "AIRT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore Memory now extracts non-conversational JSON \u2014 activity logs and system events become durable agent memory",
        "description": "`CreateEvent` accepts a new **`json` payload type** carrying \"non-conversational, JSON-formatted data\u2014such as behavioral events, activity logs, or system events\u2014**up to 100 KB for each payload**,\" and the API reference is explicit about the consequence: \"**Conversational and JSON payloads are extracted into long-term memory. Blob payloads are stored in short-term memory only and are not extracted.**\" JSON payloads run through the **semantic, user-preference, summarization and episodic** strategies. This is an extension of an existing API rather than a new service, but it changes the shape of the memory-poisoning problem in three specific ways. **The write path no longer requires a conversation.** Until now, durable agent memory grew from turns \u2014 something a user or an agent said. Now any system that can emit a `CreateEvent` writes memory directly, so the set of principals able to influence what an agent believes expands from conversational participants to every telemetry producer wired into the memory store \u2014 and the natural implementations here (activity logs, behavioural event streams, system events) are exactly the sources nobody classifies as attacker-influenceable. **It is machine-scale.** 100 KB per payload of structured data, streamed continuously, is a far higher-bandwidth channel into LLM-driven extraction than conversational turns, and extraction is the step where stored content becomes agent belief. **And the blob/JSON asymmetry is the detection rule.** A blob is inert \u2014 short-term only, never extracted; a JSON payload of the same bytes is extracted into long-term memory and will shape future sessions. So the payload *type* field is what determines whether stored data can influence the agent later, and it is the field a detection must key on rather than the payload size or source. AIDR should treat `CreateEvent` with a `json` payload as a distinct activity type from a conversational one, record the emitting principal rather than only the `actorId`, and monitor volume and provenance \u2014 a spike in JSON events from a non-conversational source is memory growth no human authored. AISPM should enumerate which memory stores accept JSON payloads, resolve which identities hold `CreateEvent` permission on each, and flag stores whose write population is broader than their conversational population; a memory store fed by system telemetry and shared across agents is a cross-agent influence channel. AIRT should test whether crafted values inside a JSON activity-log payload survive extraction into long-term memory and steer a later session",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/short-term-create-event.html",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Microsoft Foundry's Skills API is a versioned skill resource that any MCP client can load without a Foundry SDK",
        "description": "Foundry stores skills centrally through a **versioned Skills API** at `{FOUNDRY_PROJECT_ENDPOINT}/skills`, gated behind the **`Foundry-Features: Skills=V1Preview`** header, with **Foundry User** RBAC on the project. A skill is a `SKILL.md` file following the agentskills.io specification whose Markdown body \"becomes the skill's injected instructions.\" The versioning model is the useful half for posture: creating a version **auto-creates the parent skill**, **every update produces a new immutable `SkillVersion`**, and the parent `Skill` carries **`default_version`** and **`latest_version`** \u2014 so promoting a new default is a **metadata-only repoint that silently changes behaviour for every agent and toolbox referencing the skill unpinned**, and whether a given reference is pinned or floating is a discoverable, alertable property. The distribution model is what makes this more than a footnote to the toolbox finding reported 2026-08-09. Skills attached to a toolbox surface as **MCP Resources** under the **SEP-2640** Skills extension: a client calls `resources/list` once at startup and `resources/read` to pull content, and Microsoft states plainly that \"**any MCP client \u2014 GitHub Copilot, Claude Code, or your own agent harness \u2014 can consume skills this way without any Foundry SDK**.\" So Foundry becomes a **skill distribution point for non-Microsoft agents**, and instruction text authored and governed inside a Foundry project executes inside clients Foundry has no visibility into \u2014 which also means the Foundry-side inventory is the *only* place that reach is enumerable. Two further specifics belong in coverage. **The Skills API has no private-endpoint support** \u2014 a Foundry resource with public network access disabled cannot create, manage or download skills at all \u2014 so adopting skills requires a network-posture concession that is itself worth flagging. And the VS Code Toolkit ships a **prebuilt catalog** (`docx`, `pptx`, `xlsx`, `pdf`, `canvas-design`, `algorithmic-art`, `brand-guidelines`, `theme-factory`, `doc-coauthoring`, `internal-comms`, `slack-gif-creator`) registerable into a project from a card, so third-party-authored instruction content enters the estate with no procurement step. Microsoft's own caution confirms where responsibility sits: customers must understand \"the behaviors of any skills deployed and the data handling practices of any external endpoints receiving data.\" AISPM should enumerate skills and versions through `/skills`, record `default_version` versus `latest_version` and which references are pinned, read skill bodies as privilege-shaping instruction content, and reconcile Foundry-registered skills against skills observed loading in external MCP clients; AIRT should treat skill content as a persistent injection vector and specifically test whether a default-version promotion changes agent behaviour with no agent-side change. **Note: this is a coverage-gap recovery, not new platform movement** \u2014 the page carries `ms.date` 2026-08-05",
        "source": "https://learn.microsoft.com/en-us/azure/foundry/agents/how-to/tools/skills",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Devin Desktop shares terminal output and pre-commit edits with third-party ACP agent subprocesses",
        "description": "Devin Desktop **v3.8.20 (Aug 21)** adds settings that \"control whether **terminal and user-edit activity share with ACP agents**.\" What is new is the *control*, and the control is what makes the underlying flow legible \u2014 but the receiving side is the finding. **Agent Client Protocol is JSON-RPC 2.0 over stdin/stdout, and the editor launches the agent as a subprocess**, so an ACP agent is a local process with a live feed from the IDE. Devin Desktop supports **Codex, Claude Agent, OpenCode and any other ACP-compatible agent, \"including agents built by your team in-house,\"** and those third-party agents appear in the Kanban view **alongside** Devin, **share the Spaces context layer**, and \"operate with the same controls as native sessions\" \u2014 i.e. they are peers of the first-party agent, not sandboxed guests. Two data classes are therefore leaving the endpoint to a party the vendor does not operate. **Terminal output** is among the highest-value content on a developer machine: it routinely carries environment variables, connection strings, tokens echoed by CLI tools, and the contents of failed authentication attempts. **User-edit activity** is a live stream of proprietary source *before* it reaches version control, so it bypasses every control anchored to the SCM. Because ACP is an open protocol and in-house agents are explicitly supported, the destination is not enumerable from the vendor's surface \u2014 the setting's state on a given machine is the only local evidence that the flow is active, which makes it a required collection target rather than a nice-to-have. Two adjacent v3.8.20 items sharpen the scope picture: **workspace trust prompts now appear when needed to activate local agents**, confirming that local agent activation is gated on a per-workspace trust decision a developer makes unilaterally and that Zenity can read as a posture signal; and **multi-root workspaces with virtual filesystem folders** widen what one agent session can see, since a virtual root can compose directories that no single project boundary contains. Reported despite Windsurf's watching tier because this is an endpoint egress channel, not an agent convenience. ENDPOINT should detect the ACP sharing setting's state per install, enumerate attached ACP agents and resolve who operates each, treat enabled terminal-sharing on a machine holding production credentials as a high-risk condition, and inventory per-workspace trust decisions and multi-root workspace composition as agent scope attributes",
        "source": "https://docs.devin.ai/desktop/acp",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "OpenAI's official Python SDK now supports the Bedrock Runtime endpoint \u2014 the cross-provider observability break is no longer confined to Codex CLI",
        "description": "`openai-python` **v3.2.0 (Aug 17)** adds \"**Bedrock Runtime endpoint support (SDK-290)**.\" On 2026-08-21 this monitor reported the same capability arriving in **Codex CLI 0.148.0** and framed it as a coding-agent quirk that compounded two known blind spots. Its arrival in the **SDK** changes the scope of that finding materially: it is no longer a property of one client that developers choose to install, but of **the library any application uses to call OpenAI**, so cross-provider routing is now available to arbitrary production workloads rather than to interactive coding sessions. The compounding facts are unchanged and worth restating because they now apply far more broadly. Anthropic's Compliance API \u2014 the richest available source of local Claude Code and Cowork transcripts \u2014 **explicitly does not capture sessions routed through Amazon Bedrock, Google Cloud or Microsoft Foundry**. And as of 2026-08-21, `bedrock-runtime` **serves the Anthropic Messages API on its `/anthropic` route and the OpenAI shapes on `/openai/v1`**. So an application built on the OpenAI SDK, pointed at Bedrock Runtime, running a Claude model, produces traffic that OpenAI does not see, that Anthropic's compliance surface does not capture, and that arrives at AWS in a wire format whose parser selection Zenity is only now correcting to key on route path rather than hostname. The general rule this reinforces: **per-vendor telemetry cannot establish coverage once the vendors' own SDKs route to each other's endpoints \u2014 the only reliable vantage point is the cloud account's own logging.** Two smaller items in the same release stream belong in schema work rather than as separate findings: v3.2.0 also adds \"**shell call streaming events** and new service/image types,\" which implies a command-execution surface reflected in the streaming schema, and v3.1.0 (Aug 14) adds a \"**workload identity access token issued event**.\" AIDR should stop treating OpenAI-shaped traffic as OpenAI-destined, ensure AWS-side logging covers Bedrock traffic originating from OpenAI SDK clients, and add the new streaming event types to its parsers; AISPM should discover OpenAI SDK base-URL and endpoint configuration as a posture attribute, since the endpoint an application targets is a configuration choice invisible from the credential alone",
        "source": "https://github.com/openai/openai-python/releases/tag/v3.2.0",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Salesforce Winter '27's Marketing Goals Agent holds a budget and optimizes autonomously, and the second marketing agent is now named",
        "description": "Two upgrades to the caveated finding carried on 2026-08-21. The second Winter '27 marketing agent is **Agentforce Content Agent**, which creates campaign content at scale while maintaining brand consistency \u2014 so the pair is now fully named rather than half-named. More importantly, the **Marketing Goals Agent's autonomy model is now documented**, and it is stronger than last scan's \"translates objectives into strategies\": users \"define a goal, **budget**, and eligible audience,\" the agent proposes a strategy for human review and approval, and then \u2014 once activated \u2014 \"**operates autonomously within those guardrails, continuously optimizing** activities and decisions toward the desired outcome.\" That is a standing grant, not a task: a human approves once, and the agent thereafter makes ongoing spend and targeting decisions with no per-decision review. Three things follow. The **budget is a governance object**, and it is the same pattern as the AgentCore session budget reported 2026-08-21 \u2014 the ceiling a human sets is the only real bound on autonomous commercial action, which makes budget configuration a posture attribute of the first order rather than an operational setting, and makes a large or absent budget a finding in itself. The **approval boundary is one-time**, so \"was this reviewed?\" has a materially different answer than for a conversational agent; Zenity should record the activation event and its approver as the authorization for everything the agent subsequently does. And the **eligible audience is a standing data-scope grant** \u2014 a marketing agent optimizing against an audience is reading and acting on prospect and campaign data continuously, not per-request. Also confirmed this scan: **Agentforce observability** ships with refined agent analytics and **custom scorers**, some in Beta, alongside agent interoperability, improved voice and multilingual support. **The caveat from last scan stands and must be honoured**: every specific here is **WebSearch-corroborated only**. `www.salesforce.com/releases/` is still 403, the JS-rendered `help.salesforce.com` release-notes viewer still cannot be read by WebFetch, and while `agentforce/guide` recovered from its 503 it is release-agnostic and confirms nothing about Winter '27. AISPM should add both prebuilt agents to the Agentforce inventory once vendor-confirmed, treat the Marketing Goals Agent's budget and eligible audience as first-class inventory attributes with the activation approver recorded, and match on stable identifiers rather than display names given the concurrent Lead Nurturing\u2192Engagement rename",
        "source": "https://help.salesforce.com/s/articleView?id=release-notes.rn_agentforce.htm&release=262&type=5",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AWS is migrating the entire Amazon Q Developer IDE population to Kiro, an agentic IDE with per-agent MCP grants that this monitor has never scanned",
        "description": "AWS will **discontinue support for Amazon Q Developer IDE plugins on April 30, 2027** and directs users to **Kiro**, stating that \"everything you rely on today (inline suggestions, chat, and code generation) is available in Kiro,\" which adds \"agentic coding, chat and MCP support.\" The end-of-support notice itself is a deprecation and not reportable; the **named migration target is**, and Kiro's own documentation shows a surface squarely inside ENDPOINT's remit. Kiro runs \"**one unified agent harness that powers every surface (IDE, CLI, Web, and Mobile)**,\" so a single configuration follows the developer across four form factors rather than being scoped to an editor. It supports **custom agents** whose configuration files carry an **`mcpServers` field specifying which MCP servers that agent may reach**, plus a `tools` field selecting individual tools from each source and `toolAliases` for naming conflicts \u2014 meaning per-agent tool grants are declarative, file-based, and enumerable, which is good news for discovery and bad news if nobody is reading them. MCP config exists at **both workspace and user scope** (`Kiro: Open workspace MCP config` / `Open user MCP config`), and both **hot-reload from disk** via a file watcher on `.kiro/agents` directories and `mcp.json` \u2014 so an agent's capability set can change mid-session with no restart and no UI event, which defeats point-in-time capability inventory in exactly the way Anthropic's repository-mounted skills did (reported 2026-08-09). The urgency is that this is an **inherited installed base, not organic adoption**: AWS is actively pushing a population off Q Developer plugins onto Kiro by a known date. There are now **two** named Amazon successors absent from `sources.json` \u2014 **Amazon Quick** (flagged 2026-08-21) and **Kiro** \u2014 while primary-tier polling budget is spent weekly confirming that Amazon Q is still dead (Q Business closed to new customers 2026-07-31; its newest feature entry remains **Agentic RAG, August 14, 2025**; Q Developer's doc history has not moved in ~3 months). ENDPOINT should stand up Kiro detection now rather than after the migration: inventory `.kiro/agents` definitions and both MCP config scopes, resolve per-agent `mcpServers` and `tools` grants, treat hot-reload as a continuous-change surface requiring re-enumeration rather than one-time collection, and model the four surfaces (IDE, CLI, Web, Mobile) as one identity with one configuration. This scan **recommends adding `kiro.dev/docs` and its changelog to `sources.json` and demoting Amazon Q from primary-tier polling** \u2014 the second consecutive scan making that recommendation",
        "source": "https://kiro.dev/docs/mcp/",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      }
    ]
  },
  {
    "date": "2026-08-21",
    "scan_time": "2026-08-21 08:37",
    "file": "report-2026-08-21.md",
    "platforms": [
      {
        "name": "Claude Enterprise (Anthropic) \u2014 two new dated entries, Aug 18 and Aug 19",
        "status": "error",
        "details": [
          "**Browser use tool (`browser_toolset_20260801`), Aug 19.** A client toolset for driving a browser the *application* hosts, working inside a viewport rather than a whole desktop. Reads the page's **accessibility tree** via `read_page`, returning stable element references (`[ref_1]`\u2026) that Claude acts on by reference instead of coordinates; `find` locates elements from natural-language descriptions and `get_page_text` extracts visible text. Members include `navigate` (with `back`/`forward`/`reload`), the full pointer and keyboard set, `form_input`, `screenshot`, `zoom`, and tab management (`new_tab`, `list_tabs`, `switch_tab`, `close_tab`). Four members are **optional and disabled by default**: `javascript_exec`, `file_upload`, `read_console`, `read_network`. `file_upload` sets files on `<input type=\"file\">` from either executor filesystem `paths` or application-staged `document_ids`. The application must report tab inventory through `browser_state` blocks and downloads through `state_changes`. Anthropic's guidance: run the browser in a dedicated container with no credentials, enforce domain allowlisting at the network layer, refuse `javascript:`/`file:`/`data:`/`chrome:` schemes, sanitize page-supplied URLs and titles, and require human confirmation for consequential actions. Source: https://platform.claude.com/docs/en/agents-and-tools/tool-use/browser-use-tool",
          "**Computer use tool GA (`computer_toolset_20260801`), Aug 19.** No beta header; adds **batch actions** (several actions in one turn), `zoom` enabled by default, and **per-member configuration through `configs`**. Earlier beta versions remain available, and upgrading changes the request shape. Source: https://platform.claude.com/docs/en/agents-and-tools/tool-use/computer-use-tool",
          "**Agent Skills and the Skills API (`/v1/skills`) GA, Aug 19.** The `skills-2025-10-02` beta header is no longer required, including on Messages API requests that load Skills through the `container` parameter. Source: https://platform.claude.com/docs/en/build-with-claude/skills-guide",
          "**Domain restrictions for Managed Agents' `web_search` and `web_fetch`, Aug 19.** `allowed_domains` / `blocked_domains` set per-tool in the `agent_toolset_20260401` `configs` array; `web_fetch` also takes `max_content_tokens`, `web_search` takes `user_location`. Each `configs` entry is keyed by `name` and typed by an optional `type`. Source: https://platform.claude.com/docs/en/managed-agents/tools#restrict-web-search-and-web-fetch-domains",
          "**Memory stores attachable to self-hosted sandboxes, Aug 19.** The Python, TypeScript and Go SDK workers download each attached store into the customer's own sandbox at its `mount_path` and **sync the agent's changes back**. Source: https://platform.claude.com/docs/en/managed-agents/self-hosted-sandboxes#use-memory-stores",
          "**Admin API user management GA for Claude Enterprise, Aug 19.** Members, invites, groups and custom roles; the `ce-user-management-2026-07-13` beta header is no longer required. Source: https://platform.claude.com/docs/en/manage-claude/user-management"
        ],
        "tier": "primary"
      },
      {
        "name": "ChatGPT Enterprise (OpenAI) \u2014 four new dated entries, Aug 18\u201320",
        "status": "changed",
        "details": [
          "**GitLab support for Codex cloud (beta), Aug 19.** Connect GitLab projects, create cloud environments against them, **trigger tasks from issues and merge requests using `@codex`**, and **enable automatic reviews**. Setup covers webhook permissions and merge-request reviews. Source: https://learn.chatgpt.com/docs/cloud",
          "**Apple Messages plugin, Aug 20.** Read and search Messages history on the Mac, draft replies, and send through Messages. Covers **iMessage, SMS and RCS**. Requires **Full Disk Access** in System Settings plus contact-name and automation access. Ships **only in the Apple Silicon (arm64) build** of the desktop app \u2014 not web, mobile, Codex CLI or the IDE extension \u2014 and is available on all plans, installed from Plugins > Public. Sends require approval by default, with **\"Always allow sending to this chat\"** available to remove that review permanently for a conversation. Usable from both ChatGPT Work and Codex. Source: https://learn.chatgpt.com/docs/changelog",
          "**Computer History extends to the EEA, Switzerland and the UK, Aug 20**, for Pro, Business and Enterprise users \u2014 the jurisdictions explicitly excluded when the feature shipped Aug 13. Source: https://learn.chatgpt.com/docs/customization/computer-history",
          "**Codex CLI 0.148.0 adds Amazon Bedrock Runtime as a built-in provider, Aug 18**, alongside asynchronous MCP tool invocation in hooks, `/export` to Markdown, session forking and archiving, and thread credit/cost visibility in `/status`. Source: https://learn.chatgpt.com/docs/changelog",
          "*(Not reported: Site co-editing and editable Site URLs; shared read-only Codex thread snapshots; unified pinned threads; iOS 1.2026.223; Codex CLI 0.149.0 agents dashboard, `codex queue`, `/cd`-`/pwd`-`/cwd`, Vim expansion, `codex doctor`, `max`/`ultra` effort selection.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Microsoft Copilot Studio \u2014 the changelog advanced from June to July after four static scans",
        "status": "changed",
        "details": [
          "**MCP servers and workflows as tools for GitHub Copilot harness agents (preview).** Add a **workflow** or an **MCP server** as a tool to extend an agent with multistep automated processes or external services. Source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/agents-experience/tools-add-mcp-server",
          "**Entra Agent ID creation is now mandatory.** \"Starting in July 2026, Copilot Studio automatically creates a Microsoft Entra Agent ID for every new agent, and **you can no longer opt out at the environment level**.\" Previously this was a preview opt-in (Nov 2025) then a default (May 2026). Source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/admin-use-entra-agent-identities",
          "**MCP server submission for Microsoft certification (preview)** \u2014 publishers can submit an MCP server for review against Microsoft's expectations for reliability, security, compliance and responsible operation. Source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/mcp-certification",
          "**Environment-level agent telemetry export to Azure Application Insights (preview)**, configured through the Power Platform admin center. Source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/advanced-environment-level-agent-telemetry",
          "**File attachments and agent-created files (preview)** for GitHub Copilot harness agents \u2014 users attach files to a conversation and view files the agent creates during it. Source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/agents-experience/attachments-overview",
          "*(Not reported: real-time agents across voice and digital messaging with GPT-5-Chat, and Application Insights monitoring for them \u2014 an extension of the voice-agent surface reported in April and June.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Amazon Bedrock AgentCore \u2014 five new August entries",
        "status": "changed",
        "details": [
          "**Payments: x402 `upto` scheme and Machine Payments Protocol (MPP).** The `upto` scheme **pays a metered amount up to a ceiling and settles through the Permit2 contract**, in addition to the existing `exact` scheme. MPP is an HTTP-native standard using the standard authentication-challenge flow: a merchant returns `WWW-Authenticate: Payment` in a `402 Payment Required`, the agent forwards the challenge verbatim to `ProcessPayment` with `paymentType=MPP`, and **AgentCore parses the challenge, checks the session budget, signs with the wallet, and returns a ready-to-send credential** that the caller replays in an `Authorization` header. Supports `evm`, `tempo` and `solana` payment methods, with provider support varying by connector type. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/payments-process-payment.html",
          "**Payments: Quick create for Coinbase payment connectors.** AgentCore payments **provisions your Coinbase CDP credentials for you** \u2014 you authorize through Coinbase in a browser and the service creates the payment credential provider on your behalf. No API keys are generated or pasted by a human. Coinbase only; Stripe (Privy) still uses supplied credentials. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/payments-create-manager.html",
          "**Evaluations: Skill evaluators.** Two built-in tool-level evaluators \u2014 `Builtin.SkillSelectionAccuracy` (did the loaded skill fit the task, given the catalog) and `Builtin.SkillInstructionFollowing` (a five-point rating of how fully the agent followed the skill's prescribed steps). One result per skill invocation, anchored to the tool-call span that loaded the skill. Custom `TOOL_CALL` evaluators can read the same signals through the `invoked_skill`, `skill_content`, `available_skills` and `user_message` placeholders. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/skill-evaluators.html",
          "**Evaluations: Third-party evaluators.** Evaluators from the **DeepEval and AutoEval** open-source libraries, either managed (selected by ID and run by AWS on a model it operates) or derived (the library's logic run on the customer's own model and inference). Both are discoverable through `ListEvaluators` alongside built-in and custom evaluators. Source: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/third-party-evaluators.html",
          "*(Not reported: Coinbase wallet usage billed through AWS Marketplace \u2014 billing plumbing; memory/policy/harness in GovCloud (US-West) \u2014 region expansion.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Amazon Bedrock \u2014 three new entries, and a reversal of last week's guidance",
        "status": "changed",
        "details": [
          "**`bedrock-runtime` now serves the Anthropic and OpenAI API shapes, and is again the recommended endpoint (Aug 15 and Aug 19).** Endpoint guidance across the User Guide was rewritten to **recommend `bedrock-runtime` for new applications, including for OpenAI- and Anthropic-compatible workloads** \u2014 the direct opposite of the guidance captured on 2026-08-14. Concretely: Anthropic Messages API samples now target `https://bedrock-runtime.{region}.amazonaws.com/anthropic`, and the `OPENAI_BASE_URL` on **49 non-Anthropic model cards** (OpenAI, Mistral, DeepSeek, Google, Meta, Qwen, MiniMax, Moonshot, Z.AI, Writer, xAI, NVIDIA) was repointed from the `bedrock-mantle` `/v1` route to **`https://bedrock-runtime.{region}.amazonaws.com/openai/v1`**. `bedrock-mantle` \"continues to be fully supported\" and remains the only host for mantle-only features \u2014 **Web Search, Projects, Workspaces, Chat Completions on mantle, and the mantle Responses API**. Also added: **Daybreak Red: GPT-5.6 Cyber and Daybreak Blue: GPT-5.6 Sol to the Models at a glance table**. Source: https://docs.aws.amazon.com/bedrock/latest/userguide/endpoints.html",
          "**Bedrock Projects and Workspaces.** Surfaced by the endpoint work above; never inventoried by this monitor. A **Project** (OpenAI-compatible) and a **Workspace** (Anthropic-compatible) are **the same underlying resource**, managed by the **Projects API** at `POST/GET https://bedrock-mantle.{region}.api.aws/v1/organization/projects`, returning `arn:aws:bedrock-mantle:\u2026:project/proj_\u2026`. They differ only in the header used to bind inference to them: `OpenAI-Project: {project-id}` versus `anthropic-workspace: {project-id}`. Each is an **IAM resource** with tags, a `status`, and an archive lifecycle; every account has a **default workspace/project** that all inference is attributed to unless another is named. Source: https://docs.aws.amazon.com/bedrock/latest/userguide/workspaces.html",
          "**AgentCore Memory integration for Bedrock agentic retrieval (Aug 14).** Agentic retrieval on fully managed Bedrock knowledge bases now integrates with **AgentCore Memory** through a `memoryConfiguration` field on **`AgenticRetrieveStream`**. You can restore an earlier session from short-term memory, let the agent retrieve records from long-term memory, or both. Source: https://docs.aws.amazon.com/bedrock/latest/userguide/kb-test-agentic-retrieve.html"
        ],
        "tier": "primary"
      },
      {
        "name": "Google Gemini Enterprise \u2014 four dated entries, Aug 14\u201320",
        "status": "changed",
        "details": [
          "**A2UI and A2A agent registration reaches GA, with A2UI v0.9 (Aug 17).** Administrators can register agents that use **Agent to UI (A2UI)** to build custom interfaces and the **A2A Protocol** to communicate with Gemini Enterprise. v0.9 adds **Material Design components**, alongside the existing v0.8. The agent is **customer-hosted and customer-maintained**, must be network-reachable by Gemini Enterprise for A2A, and is registered by an administrator through the Cloud console or the **REST API**, which makes it available to users inside a Gemini Enterprise app. Source: https://docs.cloud.google.com/gemini/enterprise/docs/a2ui-agents/register-and-manage-an-a2ui-agent",
          "**Antigravity for IDEs (Aug 20)** \u2014 brings AI agents directly into the IDE, now available on Standard, Plus and Pay-as-you-go editions with invoiced billing accounts. Source: https://docs.cloud.google.com/gemini/enterprise/docs/release-notes",
          "**Agent Registry governs `Endpoints` as a first-class record type, and new IAM governance policies entered private preview (Aug).** The Agent Platform's Agent Registry catalogs **MCP servers, tools, agents and Endpoints**, the last being \"target destinations to centrally govern the external APIs and services that your agents can connect to\", with an **auth manager and bindings** authenticating agents to discovered tools. Source: https://docs.cloud.google.com/gemini-enterprise-agent-platform/govern/agent-registry",
          "*(Not reported: Okta SCIM autocomplete when sharing notebooks and agents; Google Identity recommended for third-party sources with OIDC/SAML external IdPs; self-serve 25-seat and invoiced 1,000-seat caps; Gemini 3.6 Flash GA in US/EU without allowlist; Gemini 3.7 Flash on mobile; team-member cards.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Cursor (watching tier) \u2014 two major capability additions, Aug 17 and Aug 19",
        "status": "changed",
        "details": [
          "**Cursor Origin \u2014 Cursor is now a code host (early beta).** \"Origin is Cursor's git forge for storing and sharing code\": repository creation, standard git operations, code browsing and search at `cursor.com/codebase`, and pull-request review and merge. Existing projects are onboarded by **GitHub mirroring**, and PRs on synced repos **sync both ways** \u2014 a comment in Cursor posts to GitHub and a reply on GitHub appears in Cursor within seconds. Integrates with **Vercel, Depot and Buildkite** for deploys and CI/CD, plus automations, cloud agents and an Origin CLI. Available on Pro, Teams and Enterprise; **teams on legacy privacy mode cannot enable it** and must switch to Privacy Mode first. Source: https://cursor.com/docs/origin",
          "**Cloud Agent subscriptions, goals, and subagents on isolated machines (Aug 19).** **Subscriptions** let agents monitor **PRs, Slack threads and scheduled tasks**, and agents **automatically subscribe to PRs they create and drive them to completion**. **`/goal`** assigns long-lived objectives that agents work toward independently. **Subagents now run on isolated machines**, each in its own virtual environment with a clean project copy. **Custom Modes** are \"always on\" skills that keep an agent focused. Non-interruptive steering queues guidance for the next tool call rather than halting execution. Source: https://cursor.com/changelog",
          "*(Not reported: Aug 13 Cloud Agent builds \u2014 reported as held on 2026-08-14.)*"
        ],
        "tier": "primary"
      },
      {
        "name": "Salesforce AgentForce \u2014 Winter '27 published on schedule, but unverifiable from any Salesforce source",
        "status": "error",
        "details": [
          "**New Agentforce marketing agents, an Agentforce Engagement rename, and a new Agentforce Builder version.** Winter '27 introduces two Agentforce agents dedicated to marketing, the first being a **Marketing Goals Agent** that translates business objectives into marketing strategies; **Agentforce Lead Nurturing is renamed Agentforce Engagement**; and a new version of **Agentforce Builder** ships. An AI **call-coaching agent** that automatically evaluates recorded calls is available with Agentforce for Sales. Salesforce's feature-availability documentation also lists Agentforce Observability, MCP interoperability, improved Agentforce Voice, and additional language support \u2014 but **MCP interoperability for Agentforce carries a release date of the week of May 18, 2026**, so it is not a Winter '27 addition and is not reported as one. Source: https://help.salesforce.com/s/articleView?id=release-notes.salesforce_release_notes.htm&release=262&type=5"
        ],
        "tier": "primary"
      }
    ],
    "findings": [
      {
        "severity": "HIGH",
        "title": "Anthropic shipped a browser-driving toolset that reads the accessibility tree and can be flagged into JavaScript execution, console and network capture",
        "description": "`browser_toolset_20260801` is a new client toolset that drives a browser the customer's own application hosts, working inside a viewport rather than a whole desktop. Its reading model is the finding's core: `read_page` returns the page's **accessibility tree** as text with stable element references (`[ref_1]`, `[ref_2]`\u2026) and Claude then acts **by reference** rather than by coordinate, with `find` resolving elements from natural-language descriptions and `get_page_text` extracting visible text. That makes the agent's interaction with a page semantic and reliable in a way screenshot-and-click never was, which raises the ceiling on what a prompt injection embedded in a page can actually accomplish. **Anthropic's own documentation leads with that risk** \u2014 it instructs implementers to treat every page-supplied string (titles, URLs, text) as untrusted, sanitize before returning it to Claude, refuse `javascript:`/`file:`/`data:`/`chrome:` schemes in the navigate handler, enforce domain allowlisting **at the network layer**, run the browser in a dedicated container with no credentials, and require human confirmation for purchases, account modifications and messaging. Four members are optional and off by default, and each one is a distinct escalation an implementer can silently enable: **`javascript_exec`** runs arbitrary JavaScript in page context, **`read_console`** and **`read_network`** expose logs and requests that routinely carry tokens and secrets (Anthropic explicitly says to redact before enabling them), and **`file_upload`** sets files on `<input type=\"file\">` from either executor filesystem `paths` or application-staged `document_ids` \u2014 with the warning that a malicious page could otherwise direct uploads of any accessible file. AIRT should build page-hosted injection tests against reference-based interaction specifically, and test whether a hostile page can induce navigation to a non-HTTP scheme, an unapproved consequential action, or a `file_upload` outside its intended directory. AISPM should discover which applications enable this toolset and, per deployment, which optional members are turned on, treating `javascript_exec` and `file_upload` as privileged configuration; AIDR should ingest `browser_state` tab inventories and `state_changes` download events as agent activity, since a download reported there is data leaving a page and landing on the executor",
        "source": "https://platform.claude.com/docs/en/agents-and-tools/tool-use/browser-use-tool",
        "module": "AIRT",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "The ChatGPT Mac app can now read and send iMessage, SMS and RCS, and requires Full Disk Access to do it",
        "description": "OpenAI shipped an **Apple Messages plugin** that lets ChatGPT search a user's entire Messages history, summarize past conversations, draft replies, and **send messages** through Messages. It covers **iMessage, SMS and RCS**, so it reaches SMS-delivered content including one-time passcodes and MFA codes. The permission requirement is the finding: the integration needs **Full Disk Access** in macOS System Settings, plus access to contact names and automation tooling \u2014 a grant that extends far beyond Messages and that endpoint policy at most enterprises treats as high-privilege. Two more properties matter for coverage. First, the **send-approval model degrades by design**: sends require user approval by default, but users can choose **\"Always allow sending to this chat\"**, which OpenAI itself warns removes the final review before ChatGPT sends on the user's behalf \u2014 so a per-conversation toggle silently converts a human-in-the-loop control into autonomous outbound messaging. Second, availability is **narrower than \"the ChatGPT desktop app\"**: the plugin ships only in the **Apple Silicon (arm64)** build and does nothing on Intel Macs, the web, mobile, Codex CLI or the IDE extension \u2014 reinforcing that per-OS-and-architecture feature modelling is now required rather than optional. It is available on all plans and installed from Plugins > Public, and works from **both ChatGPT Work and Codex**, so it is reachable from a coding agent as well as the chat client. ENDPOINT should detect the plugin's installed and enabled state per device, detect the Full Disk Access grant to the ChatGPT app as a distinct high-risk condition, and inventory which conversations have persistent send approval; AIDR should treat agent-originated messages as an outbound data-flow channel; AIRT should test whether content arriving in a message thread can steer the agent into sending on the user's behalf where persistent approval is already granted",
        "source": "https://learn.chatgpt.com/docs/changelog",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Codex now connects to GitLab with `@codex` triggers on issues and merge requests and automatic reviews",
        "description": "OpenAI extended Codex cloud beyond GitHub. You can now **connect GitLab projects**, create cloud environments against them, **trigger tasks by mentioning `@codex` on issues and merge requests**, and **enable automatic reviews**. This is a new code-host integration surface, not a variation on an existing one, and it matters for three reasons. It is a **second source-control identity to discover and scope**: setup covers webhook permissions, so Codex holds a standing credential and webhook grant against GitLab projects, and the effective blast radius is whatever that credential can read and write across connected projects. It **moves the trigger boundary out of the developer's hands**: with `@codex` mentions and automatic reviews enabled, an agent task can be started by anyone who can comment on an issue or open a merge request \u2014 including, on projects that accept outside contributions, someone who is not an employee. And it **extends the injection path**: issue and merge-request bodies are attacker-influenceable text that now reaches an agent with repository access, the same pattern Zenity should already be testing on the GitHub side. Note also that self-hosted GitLab remains reachable through the GitLab MCP server as a separate path, so a customer may have both a first-party Codex-GitLab connection and an MCP-mediated one. AISPM should discover Codex-to-GitLab connections, resolve the authenticating identity and its project scopes, enumerate which projects have automatic reviews enabled, and flag connections on projects that accept external contributions; AIRT should test `@codex` mention-triggered injection from issue and merge-request content",
        "source": "https://learn.chatgpt.com/docs/cloud",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Copilot Studio agents can now take an MCP server or a workflow as a tool on the GitHub Copilot harness",
        "description": "The July 2026 Copilot Studio release adds the ability to attach a **workflow** or an **MCP server** as a tool to agents built on the **GitHub Copilot harness** \u2014 the new orchestration runtime that went production-ready preview in June. Both are new tool types on a new harness, which means the tool-attachment surface Zenity inventories for Copilot Studio is no longer complete. The MCP case is the more consequential: an MCP server attached as a tool brings an external, independently-operated endpoint into the agent's capability set, with its own tool list, its own authentication, and its own change cadence that no Copilot Studio review gate observes after attachment. The workflow case is a **privilege-composition** problem \u2014 a workflow is a multistep automated process that may itself hold connections and credentials, so attaching one grants the agent the workflow's reach rather than just a single action, and the agent's effective permissions become the union of its own and every attached workflow's. This lands on a platform where the harness itself is new, so agents on the GitHub Copilot harness should be treated as a distinct inventory class from standard-harness agents: they alone can hold MCP servers, workflows, skills, memory, Microsoft IQ and Foundry IQ connections, and file attachments. AISPM should enumerate agents by harness, discover attached MCP servers and resolve each to its endpoint, transport, authentication and advertised tool list, expand attached workflows to the connections and credentials they carry, and flag agents whose effective permissions grow through workflow attachment rather than direct grant. AIRT should test whether an attached MCP server's tool descriptions can inject instructions into the agent",
        "source": "https://learn.microsoft.com/en-us/microsoft-copilot-studio/agents-experience/tools-add-mcp-server",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "AgentCore agents can now settle open-ended, metered payments autonomously via the Machine Payments Protocol and the x402 `upto` scheme",
        "description": "AgentCore payments added two payment modes that move agents from fixed-price transactions to open-ended commercial commitments. The **x402 `upto` scheme** \"pays a metered amount up to a ceiling and settles through the Permit2 contract\" \u2014 meaning the agent authorizes a range rather than an amount, and the final figure is determined by the counterparty's metering, not by the agent's own decision at authorization time. The **Machine Payments Protocol** is the larger change: a merchant returns a `WWW-Authenticate: Payment` challenge in a `402 Payment Required` response, the agent **forwards that challenge verbatim** to `ProcessPayment` with `paymentType=MPP`, and AgentCore **parses the challenge, checks the session budget, signs with the wallet, and returns a ready-to-send credential** which the caller replays in an `Authorization` header. Two things follow. First, **an untrusted party's HTTP response is the input that drives a wallet signature** \u2014 the challenge is merchant-supplied and forwarded unmodified, so the integrity of the payment depends on AgentCore's parsing and on the session budget being the only real ceiling. Second, the **session budget becomes the primary financial control**, which makes budget configuration a posture attribute of the first order rather than an operational setting. MPP supports the `evm`, `tempo` and `solana` payment methods with provider support varying by connector, so settlement can land on multiple chains from one agent. AIRT should test whether a manipulated or malicious `402` challenge can induce an over-payment, a payment to an unintended recipient, or a signature outside the intended scheme, and should probe the boundary between the `upto` ceiling and the session budget. AISPM should inventory payment managers, connectors, payment instruments and wallets as financial entities, record each session's configured budget and flag unbudgeted or high-ceiling sessions, and enumerate which agents hold payment capability at all; AIDR should treat `ProcessPayment` calls as a high-severity activity type with the counterparty, scheme and amount as required fields",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/payments-process-payment.html",
        "module": "AIRT",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "AWS moved the OpenAI and Anthropic API shapes onto `bedrock-runtime` and reversed last week's endpoint recommendation \u2014 endpoint no longer predicts wire format",
        "description": "On August 15 and 19 AWS rewrote endpoint guidance across the Bedrock User Guide to **recommend `bedrock-runtime` for new applications, including for OpenAI- and Anthropic-compatible workloads** \u2014 the direct reversal of the guidance this monitor captured on 2026-08-14, which recommended `bedrock-mantle`. The substantive part is not the recommendation but the **routing change underneath it**. Anthropic Messages API samples now target `https://bedrock-runtime.{region}.amazonaws.com/anthropic`, and the `OPENAI_BASE_URL` on **49 non-Anthropic model cards** (OpenAI, Mistral, DeepSeek, Google, Meta, Qwen, MiniMax, Moonshot AI, Z.AI, Writer, xAI, NVIDIA) was repointed from the mantle `/v1` route to **`https://bedrock-runtime.{region}.amazonaws.com/openai/v1`**. The consequence for detection is direct and immediate: **both Bedrock endpoints now serve foreign API shapes**, so a parser selected by hostname will mis-handle traffic, and last week's coverage recommendation \u2014 add OpenAI Responses, Chat Completions and Anthropic Messages parsers *for the mantle endpoint* \u2014 is now insufficient rather than wrong. Those shapes must be handled on `bedrock-runtime`'s `/anthropic` and `/openai/v1` routes too, and the authentication differs by route (a short-term bearer token from `aws-bedrock-token-generator` on one path, SigV4 via `AnthropicBedrock` on another). `bedrock-mantle` \"continues to be fully supported\" and remains the **only** host for mantle-only features \u2014 **Web Search, Projects, Workspaces, Chat Completions on mantle, and the mantle Responses API** \u2014 so neither endpoint can be dropped from coverage. AIDR should key parser selection on the **route path**, not the hostname, and cover `/anthropic`, `/openai/v1` and the mantle `/v1` routes; AISPM should re-inventory which endpoints and routes each workload actually reaches, since AWS's reversal means new applications built this week will look different from ones built last week",
        "source": "https://docs.aws.amazon.com/bedrock/latest/userguide/endpoints.html",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Gemini Enterprise took customer-hosted A2UI/A2A agent registration to GA, letting external agents render their own UI inside the tenant",
        "description": "Registering agents that use the **Agent2Agent (A2A) Protocol** together with **Agent to UI (A2UI)** is now generally available, with **A2UI v0.9** adding Material Design components alongside v0.8. The architecture is what makes this a first-order inventory problem: the agent is **hosted and maintained by the customer or a third party, not by Google**, must be network-reachable by Gemini Enterprise for A2A communication, and is registered by a **Gemini Enterprise administrator** through the Cloud console or the **REST API** \u2014 after which it becomes available to users inside a Gemini Enterprise app. So a registered A2UI agent is an entity whose code, model, tools and data handling live entirely outside the platform Zenity is monitoring, while its output is rendered to users inside the trusted enterprise surface. A2UI compounds that: the external agent supplies **interface components**, not merely text, so it controls what the user sees and interacts with inside Gemini Enterprise \u2014 buttons, forms and inputs defined by a remote party. That is a phishing and consent-manipulation surface that a text-only agent does not have, and the v0.8-to-v0.9 span means two component protocols must be reasoned about. AISPM should discover registered A2A/A2UI agents through the REST API, record each agent's endpoint, owner, hosting location, A2UI version and declared capabilities, distinguish externally-hosted agents from Google-hosted ones as a distinct risk class, and flag agents registered outside a review process; AIRT should test whether an A2UI-supplied component can misrepresent a consequential action or harvest input, and whether A2A responses from a registered agent are treated as trusted instruction by the orchestrator",
        "source": "https://docs.cloud.google.com/gemini/enterprise/docs/a2ui-agents/register-and-manage-an-a2ui-agent",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Cursor now hosts source code \u2014 Origin is a git forge with bidirectional GitHub sync",
        "description": "Cursor has shipped **Origin**, \"Cursor's git forge for storing and sharing code\" (early beta), moving a watching-tier IDE vendor into the code-hosting business. It supports repository creation, standard git operations, code browsing and search at `cursor.com/codebase`, and pull-request review and merge. Existing projects arrive by **GitHub mirroring**, and PRs on synced repos **sync bidirectionally** \u2014 a comment in Cursor posts to GitHub, and a reply on GitHub appears in Cursor within seconds. It integrates with **Vercel, Depot and Buildkite** for deployment and CI/CD, and with Cursor's own automations and cloud agents, plus an Origin CLI. This crosses the threshold for a watching-tier report because it is not an agent feature \u2014 it is a **new location where the customer's source code lives**, outside the sanctioned SCM, populated by a mirror that most organizations will not have approved as a code destination. Three specifics are worth capturing. The **bidirectional PR sync** means code review moves off the governed platform: an approval or comment recorded in Cursor is authoritative on GitHub, so branch-protection and review-audit assumptions anchored to GitHub no longer hold. The **CI/CD integrations** mean an Origin-hosted repo can reach deployment infrastructure directly. And the **privacy-mode interaction is a real control**: Origin requires Privacy Mode and \"teams on legacy privacy mode cannot enable Origin\", so whether a team switched modes to adopt it is a discoverable posture signal. Available on Pro, Teams and Enterprise. AISPM should detect Origin-hosted and Origin-mirrored repositories as a code location outside sanctioned SCM, resolve which namespaces are claimed and who holds repository access, and flag repositories whose PR review path runs through Cursor; ENDPOINT should detect Origin CLI installation on developer machines",
        "source": "https://cursor.com/docs/origin",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "HIGH",
        "title": "Anthropic Agent Skills and the Skills API went GA, making a skill an enumerable first-class API resource",
        "description": "Agent Skills and the **Skills API (`/v1/skills`)** are now generally available on the Claude API, with the `skills-2025-10-02` beta header no longer required \u2014 including on Messages API requests that load Skills through the **`container`** parameter. The governance significance is that a skill has graduated from a beta-gated experiment to a durable, addressable resource with its own CRUD endpoint, which means two things at once. It is now a **discoverable inventory entity type**: `/v1/skills` gives Zenity a direct API path to enumerate every skill in an organization, its content and its metadata, without inferring skills from transcripts. And it is now **broadly reachable**: removing the beta header removes the friction that limited adoption, so skill counts should be expected to grow, and skills loaded through the `container` parameter attach to a session at runtime rather than being fixed in an agent's definition. A skill is instruction text that becomes part of the agent's operating context, so it is both a privilege-shaping object and an injection surface \u2014 and this GA lands in the same week that AgentCore shipped skill evaluators and Copilot Studio and Gemini Enterprise both expanded their own skill features, so \"skill\" is converging into a cross-platform entity type that Zenity should model once and map per platform. Note the cross-reference to a known blind spot: Anthropic's Compliance API transcripts include skill content only *when the client sends it*, so `/v1/skills` is the authoritative source and transcripts are not. AISPM should enumerate skills through `/v1/skills` as an inventory entity with owner, content and attachment points, inspect skill bodies for embedded credentials and for instructions that widen tool use, and reconcile skills observed at runtime through `container` against the registered set; AIRT should treat skill content as a persistent injection vector",
        "source": "https://platform.claude.com/docs/en/build-with-claude/skills-guide",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Copilot Studio now creates an Entra Agent ID for every new agent and has removed the environment-level opt-out",
        "description": "As of July 2026, \"Copilot Studio automatically creates a Microsoft Entra Agent ID for every new agent, and **you can no longer opt out at the environment level**.\" This completes a three-stage rollout \u2014 preview opt-in in November 2025, default-on in May 2026, now mandatory \u2014 and it is unusual among this report's findings in being **net-positive for discovery**: every new Copilot Studio agent is now guaranteed to exist as an addressable directory principal, so the Entra agent registry becomes a complete enumeration of new agents rather than a partial one, and Conditional Access, connector permission scoping and DLP can be applied per agent. Two caveats define the coverage work. First, **\"every new agent\" is not \"every agent\"**: agents created before the mandate in environments that had opted out will still lack an Agent ID, so a population of identity-less legacy agents persists and is exactly the set that a registry-based inventory will miss. That mirrors the `instance_identity`-null legacy-agent pattern already documented on the Foundry side, and the two should be reconciled with the same logic. Second, the removal of the opt-out means an **identity is created whether or not the maker or the tenant wanted one**, so agent-identity growth is now automatic and the count of Entra agent principals will rise with every agent built \u2014 making unused, orphaned and ownerless agent identities a maintenance problem that will grow on its own. AISPM should treat the Entra agent registry as authoritative for post-July Copilot Studio agents, separately enumerate pre-mandate agents lacking an Agent ID as a known-incomplete population, and reconcile both against Copilot Studio's own agent inventory schema so identity-less agents are visible rather than absent",
        "source": "https://learn.microsoft.com/en-us/microsoft-copilot-studio/admin-use-entra-agent-identities",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Bedrock Projects and Workspaces are an uninventoried control-plane isolation boundary managed by a Projects API",
        "description": "Surfaced by the endpoint-guidance rewrite and never covered by this monitor, **Projects** (OpenAI-compatible) and **Workspaces** (Anthropic-compatible) are **the same underlying resource** under two names, managed by a single **Projects API** at `https://bedrock-mantle.{region}.api.aws/v1/organization/projects` and returning ARNs of the form `arn:aws:bedrock-mantle:\u2026:project/proj_\u2026`. They differ only in the header that binds inference to them \u2014 **`OpenAI-Project: {project-id}`** versus **`anthropic-workspace: {project-id}`**. Each carries a `name`, a `status`, AWS **tags**, an archive lifecycle, and is addressable **as a resource in IAM policies**, so a project is simultaneously an access-control boundary, a cost-allocation unit and an observability scope. Three points for coverage. This is a **new AWS entity type** with its own CRUD API that Zenity's Bedrock discovery does not enumerate, and AWS's recommended structure \u2014 one project per application per environment \u2014 means a mature customer will have many of them describing the real shape of their AI estate. The **header-based binding** means the project an inference request is attributed to is asserted **by the caller at request time**, not derived from the credential, so an inventory built only from control-plane state will not tell you which project traffic actually claimed. And **every account has a default project/workspace that all inference lands in unless another is named** \u2014 traffic resolving to the default is a strong signal of a workload that was never deliberately scoped, the same posture finding as Anthropic's Default Workspace. Note the naming collision worth guarding against: a Bedrock Workspace is not an Anthropic workspace, and `anthropic-workspace` here carries an AWS `proj_` ID. AISPM should enumerate projects through the Projects API, record tags, IAM policies and archive state, correlate the `OpenAI-Project` and `anthropic-workspace` headers observed in traffic against the enumerated set, and flag production inference attributed to the default project",
        "source": "https://docs.aws.amazon.com/bedrock/latest/userguide/workspaces.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore skill evaluators confirm skills are a catalog-backed AgentCore entity and expose per-invocation skill signals",
        "description": "AgentCore Evaluations added two built-in **tool-level** evaluators for agents that use skills: **`Builtin.SkillSelectionAccuracy`**, which judges whether the skill the agent loaded fits the task **given the catalog of available skills**, and **`Builtin.SkillInstructionFollowing`**, which rates on five points how fully the agent followed the loaded skill's prescribed steps. Results are produced **one per skill invocation** and **anchored to the tool-call span that loaded the skill**, and they work with on-demand, batch and online evaluations. Two things make this reportable beyond the evaluators themselves. It **confirms the shape of skills on AgentCore**: there is a catalog of available skills, a skill is *loaded* at runtime through a tool call, and a skill has prescribed steps \u2014 so skills are a discoverable AgentCore entity with a catalog to enumerate and a runtime attachment event to observe, joining Anthropic's `/v1/skills`, Copilot Studio skills and Gemini Enterprise skills in this same week. And it hands Zenity **ready-made detection signals**: custom `TOOL_CALL` evaluators can read the **`invoked_skill`, `skill_content`, `available_skills` and `user_message`** placeholders, which is precisely the join \u2014 what the user asked, what skills were on offer, which was chosen, and what it contained \u2014 that a detection for skill-driven privilege abuse needs, and it is available without building new instrumentation. AISPM should enumerate the AgentCore skill catalog and treat a skill as an inventory entity with content; AIDR should ingest skill-load tool-call spans as an activity type and consider deriving custom evaluators over the four placeholders to detect a mismatch between user intent and loaded skill",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/skill-evaluators.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore Coinbase quick create provisions blockchain wallet credentials with no human ever handling the key",
        "description": "When creating a Coinbase payment connector, **Quick create** now has AgentCore payments \"provision your Coinbase CDP credentials for you\" \u2014 the operator authorizes access through Coinbase in a browser, and the service **creates the payment credential provider on their behalf**, with no Coinbase API keys generated or pasted by a person. This is a credential-provenance change on a financial capability, and it cuts both ways in a manner Zenity should represent explicitly. It **improves hygiene**: a key that no human ever sees cannot be pasted into a ticket, committed to a repository or shared in chat, which removes the most common failure mode for wallet credentials. It **degrades attribution and reviewability**: the resulting credential provider has no human custodian, its creation is a browser-authorization event rather than a key-management action, and an organization inventorying \"who holds wallet credentials\" by looking at key custody will find nobody \u2014 the answer is the service. Combined with the MPP finding above, the composite picture is an agent that can hold a wallet whose credentials were auto-provisioned and settle metered payments bounded only by a session budget. The asymmetry is also worth recording as a discovery rule: quick create is **Coinbase-only**, while Stripe (Privy) connectors continue to use customer-supplied credentials, so connector type predicts credential provenance. AISPM should inventory payment credential providers with their provenance (quick-created versus supplied), attribute each to the identity that performed the browser authorization rather than to a key holder, and treat a quick-created provider with no named owner as an ownership gap on a financial resource",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/payments-create-manager.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore Evaluations now runs third-party DeepEval and AutoEval logic, managed or on the customer's own model",
        "description": "AgentCore Evaluations added evaluators drawn from the **DeepEval and AutoEval** open-source libraries, usable two ways: select a **managed** third-party evaluator by ID and AWS runs it on a model it operates, exactly like a built-in; or **derive** a custom evaluator from a built-in or third-party evaluator and run its logic on the customer's own model and inference. Both managed and derived third-party evaluators are **discoverable through `ListEvaluators` alongside built-in and custom evaluators**. The coverage point is that the evaluation pipeline \u2014 which reads agent inputs, outputs, tool calls and tool results in order to score them \u2014 now executes **third-party open-source logic**, and `ListEvaluators` deliberately presents built-in, custom and third-party evaluators in one undifferentiated list. So the provenance of the code judging an agent's behaviour is no longer evident from the inventory surface, and an organization can be running community-maintained evaluation logic over its most sensitive agent traffic without that being visible as a distinct fact. The managed variant additionally means the evaluation runs on **AWS-operated models** rather than the customer's, which is a data-flow the customer did not separately choose. AISPM should enumerate configured evaluators through `ListEvaluators` and **classify each by provenance** \u2014 built-in, custom, managed third-party, or derived \u2014 flagging third-party evaluators as a supply-chain dependency in the evaluation path, and should record whether a managed evaluator sends agent content to an AWS-operated model",
        "source": "https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/third-party-evaluators.html",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Anthropic memory stores now sync into and out of customer-hosted self-hosted sandboxes",
        "description": "Claude Managed Agents sessions running in a **self-hosted sandbox** can now attach **memory stores**, and the mechanism is a genuine bidirectional data flow rather than a read: the Python, TypeScript and Go SDK workers **download each attached store into the sandbox at its `mount_path`** and **sync the agent's changes back to the store**. This creates a data path that crosses the boundary between Anthropic-managed state and customer-controlled compute in both directions, and it is the kind of flow that is easy to miss because neither endpoint is new \u2014 memory stores existed and self-hosted sandboxes existed; what is new is the sync between them. Three consequences. Memory content now **lands on disk inside customer infrastructure** at a known mount path, so it becomes an on-host artifact subject to the customer's own backup, logging and access controls rather than only Anthropic's. Changes made inside the sandbox \u2014 including anything the agent writes, and anything a compromised sandbox writes \u2014 **propagate back into the durable memory store** and therefore into future sessions, making the store a persistence mechanism across sessions. And because the sandbox is customer-operated, the integrity of what returns to the store depends on controls Anthropic does not enforce. AIDR should treat memory-store attachment, download and sync-back as distinct activity events and monitor for unexpected write volume; AISPM should discover which agents attach which memory stores to self-hosted sandboxes, record `mount_path` locations as sensitive on-host data locations, and treat a memory store shared across agents or sessions as a cross-session influence channel worth flagging",
        "source": "https://platform.claude.com/docs/en/managed-agents/self-hosted-sandboxes#use-memory-stores",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Anthropic added per-tool domain allowlists and blocklists for Managed Agents' web search and web fetch",
        "description": "Managed Agents can now restrict which sites the `web_search` and `web_fetch` tools reach, by setting **`allowed_domains` or `blocked_domains`** on the tool's entry in the `agent_toolset_20260401` **`configs`** array; `web_fetch` additionally accepts **`max_content_tokens`** and `web_search` accepts `user_location`. Each `configs` entry is identified by its `name` and typed by an optional `type`, and entries passing only `name`, `enabled` and `permission_policy` keep working \u2014 so this is an additive extension of a configuration surface Zenity should already be reading. This is a **control** rather than a new capability, which is exactly why it belongs in posture coverage: the presence, absence and content of these lists is now a measurable property of every Managed Agent with web access. An agent with `web_fetch` and no `allowed_domains` can retrieve arbitrary internet content into its context, which is the primary indirect-injection path for this class of agent; an agent with a tight allowlist is materially safer, and that difference is discoverable. `max_content_tokens` is a secondary signal \u2014 it bounds how much untrusted retrieved content can enter the context per fetch. The typed-`configs` change also means Zenity's parsing of this array should not assume the old three-key shape. AISPM should read the `configs` array per agent, record whether each web tool is domain-restricted and by which list, flag web-enabled agents with no restriction as an elevated injection-exposure class, and treat a change that widens or removes an allowlist as a posture regression worth alerting on",
        "source": "https://platform.claude.com/docs/en/managed-agents/tools#restrict-web-search-and-web-fetch-domains",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Anthropic's Claude Enterprise user-management Admin API went GA \u2014 members, invites, groups and custom roles",
        "description": "The **Admin API** user-management endpoints for **Claude Enterprise (claude.ai) organizations** \u2014 members, invites, **groups** and **custom roles** \u2014 are now generally available, with the `ce-user-management-2026-07-13` beta header no longer required. This is the identity and authorization surface for the enterprise Claude tenant, and its GA matters to Zenity for a straightforward reason: it is now a stable, non-beta API path to enumerate **who is in the organization, what groups they belong to, and what custom roles grant them** \u2014 the join Zenity needs to attribute observed agent activity to a governed identity and to evaluate whether that identity's privileges are appropriate. **Custom roles** are the part deserving specific attention: a custom role is a customer-defined permission set, so the meaning of a role is not fixed across tenants and cannot be inferred from its name \u2014 Zenity must read the role's actual permissions rather than pattern-matching a label, and an over-broad custom role is a posture finding that only appears if the permissions are resolved. Groups matter as the mechanism through which access is granted at scale, so a group's membership is the real blast radius of any grant made to it. Read alongside the Compliance API local-session work from 2026-08-14, this completes a usable pair: the Compliance API supplies agent activity keyed on `user.id`, and the Admin API resolves that ID to a member, their groups and their effective roles. AISPM should enumerate members, invites, groups and custom roles, resolve each custom role to its concrete permissions, flag over-privileged roles and stale invites, and join identities to Compliance API session activity",
        "source": "https://platform.claude.com/docs/en/manage-claude/user-management",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Copilot Studio can export environment-level agent telemetry to Azure Application Insights",
        "description": "Copilot Studio added a preview capability to export **environment-level agent telemetry** to **Azure Application Insights**, configured through the **Power Platform admin center**. This is an ingestion opportunity and a posture attribute in one. As ingestion: it is a first-party, admin-configured, environment-wide path for Copilot Studio agent telemetry to reach a destination Zenity can already read, rather than requiring per-agent instrumentation or scraping the Copilot Studio UI \u2014 and because it is scoped to the environment, enabling it once covers every agent in that environment, including agents created later. As posture: whether an environment has telemetry export configured determines whether agent behaviour there is observable at all, so an environment **without** it is a monitoring blind spot that Zenity should report as a gap in its own right, particularly for environments hosting autonomous or externally-facing agents. Two practical notes. Because configuration happens in the Power Platform admin center rather than per agent, the setting lives with the platform administrator and is discoverable at tenant scope. And the Foundry precedent from 2026-08-14 should be applied here rather than rediscovered: agent telemetry carrying prompts, tool definitions, tool arguments and tool results is sensitive content, and Foundry is relocating exactly those attributes into a protected `AppGenAIContent` table by September 30, 2026 \u2014 so Zenity should establish up front which Application Insights tables Copilot Studio's export writes to, and who can read them. AIDR should add Copilot Studio's Application Insights export as an ingestion source; AISPM should discover which environments have it enabled and flag those that do not",
        "source": "https://learn.microsoft.com/en-us/microsoft-copilot-studio/advanced-environment-level-agent-telemetry",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Microsoft opened an MCP server certification program for Copilot Studio",
        "description": "Publishers can now **submit an MCP server for Microsoft certification**, to \"give customers and administrators confidence that it meets Microsoft's expectations for reliability, security, compliance, and responsible operation\" (preview). This creates a **new trust attribute** on an entity type Zenity already inventories, and it arrives in the same release that lets Copilot Studio agents attach MCP servers as tools \u2014 so certification status becomes a usable posture signal precisely when MCP attachment becomes easy. The parallel to draw is Foundry's connector publisher tiers, reported 2026-08-14, where Microsoft's own documentation distinguishes internal, external, verified third-party and independent publishers \"at a lower certification bar\", and where the portal gated only validated connectors while the REST/SDK path had no gating at all. The same question applies here and should be answered rather than assumed: whether an uncertified MCP server can still be attached, and through which paths. Certification is also a **point-in-time attestation about a continuously-changing remote service** \u2014 an MCP server's tool list, prompts and backing implementation can change after certification without re-review \u2014 so the attribute should be recorded with its date and treated as weaker evidence than a live inspection of the server's advertised tools. AISPM should record certification status and date per attached MCP server as an inventory attribute, flag uncertified servers attached to agents holding sensitive connections, and detect changes in a certified server's advertised tool list as a signal that the attestation no longer describes what is deployed",
        "source": "https://learn.microsoft.com/en-us/microsoft-copilot-studio/mcp-certification",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "AgentCore Memory is now wired into Bedrock knowledge-base agentic retrieval via `memoryConfiguration`",
        "description": "Agentic retrieval on fully managed Amazon Bedrock knowledge bases now integrates with **AgentCore Memory** through a **`memoryConfiguration`** field on the **`AgenticRetrieveStream`** API. Three modes are documented: restore an earlier session from **short-term memory**, let the agent **retrieve memory records from long-term memory**, or both. This is a cross-service data flow between two surfaces Zenity tracks separately, and the direction is the part that matters \u2014 **memory content flows into the retrieval path**, so what an agent retrieves from a knowledge base is now conditioned on persisted memory from prior sessions. Two consequences follow. **Retrieval results become session-history-dependent**, meaning the same query against the same knowledge base can return different content depending on what memory was attached, which breaks the assumption that a knowledge base's effective exposure can be evaluated from its own permissions alone. And **memory becomes a durable influence channel on retrieval**: a record written to long-term memory in one session shapes retrieval in later ones, so memory poisoning has a retrieval-side effect and not merely a conversational one. The session-restore mode additionally means conversation state from an earlier session is re-hydrated into a new one, which is a data-lineage question for any organization that believes sessions are isolated. AIDR should treat `AgenticRetrieveStream` calls carrying `memoryConfiguration` as a joined memory-plus-retrieval activity and record both the memory resource and the knowledge base; AISPM should map which AgentCore Memory resources are attached to which knowledge bases, and flag long-term memory feeding retrieval over sensitive knowledge bases",
        "source": "https://docs.aws.amazon.com/bedrock/latest/userguide/kb-test-agentic-retrieve.html",
        "module": "AIDR",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Cursor Cloud Agents now self-subscribe to PRs and Slack threads, run scheduled tasks, and spawn subagents on isolated machines",
        "description": "Cursor's August 19 Cloud Agents release adds autonomy primitives that change what a Cursor agent is, for a watching-tier platform. **Subscriptions** let agents monitor **PRs, Slack threads and scheduled tasks**, and agents **automatically subscribe to PRs they create and drive them to completion** \u2014 so an agent's work is no longer bounded by a human-initiated session; it acquires a standing trigger on artifacts it produced itself. **`/goal`** assigns long-lived objectives that agents \"work toward independently\". **Subagents now run on isolated machines**, each in its own virtual environment with a clean project copy, which multiplies the compute identities acting on a repository from one per task to many. **Custom Modes** are described as \"always on\" skills. Non-interruptive steering queues human guidance for the next tool call instead of halting execution \u2014 a small but real reduction in the human's ability to stop work in progress. The coverage consequences are: agent activity becomes **continuous and event-triggered rather than session-bounded**, so an inventory of \"agents\" is less meaningful than an inventory of **active subscriptions and goals**; **Slack thread monitoring** puts an agent on a channel where untrusted text arrives, which is an injection path into an agent holding repository access; and subagents on isolated machines mean the principal acting on code is frequently not the one a human authorized. AISPM should discover Cursor Cloud Agent subscriptions, goals and scheduled tasks as standing-authorization entities, resolve which repositories and Slack workspaces each reaches, and flag self-created PR subscriptions as autonomous action; AIRT should test Slack-thread-sourced injection into subscribed agents",
        "source": "https://cursor.com/changelog",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Codex CLI added Amazon Bedrock Runtime as a built-in provider, compounding a documented Anthropic capture blind spot",
        "description": "Codex CLI 0.148.0 added **Amazon Bedrock Runtime as a built-in provider**, so a developer can point OpenAI's coding agent at Bedrock-hosted models with no third-party shim. On its own this is a routing convenience; in combination with two facts already established in this monitor it is a coverage problem worth naming. First, Anthropic's Compliance API \u2014 the surface reported on 2026-08-14 as the richest available source of local Claude Code and Cowork session transcripts \u2014 **explicitly does not capture Claude Code routed through Amazon Bedrock, Google Cloud or Microsoft Foundry**. Second, as of this scan `bedrock-runtime` **serves the Anthropic Messages API directly** on its `/anthropic` route. So a developer using Codex CLI against Bedrock Runtime with a Claude model produces agent activity that is invisible to Anthropic's compliance surface, arrives at AWS in Anthropic's wire format on an endpoint whose parser selection Zenity is only now correcting, and originates from OpenAI's client rather than Anthropic's. Each vendor's own telemetry sees at most a fragment. The general lesson is that **built-in cross-provider routing in local agent clients breaks per-vendor observability**, and the mitigation is endpoint-side rather than client-side: coverage has to come from the AWS account's own logging, not from either model vendor. Also in the same release: **asynchronous MCP tool invocation in hooks**, which lets an MCP tool call run without blocking hook execution. ENDPOINT should detect Codex CLI provider configuration on developer machines and identify Bedrock-routed configurations as a case where vendor-side compliance capture does not apply; AIDR should ensure Bedrock-side logging covers traffic originating from Codex CLI",
        "source": "https://learn.chatgpt.com/docs/changelog",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "OpenAI Computer History is now available in the EEA, Switzerland and the UK",
        "description": "**Computer History** \u2014 the macOS feature reported on 2026-08-14 that turns interaction events across every app and website into memories via an ephemeral Codex session, requiring macOS accessibility access and writing plain-text memory files to local disk \u2014 is now available to **Pro, Business and Enterprise users in the EEA, Switzerland and the UK**, the three jurisdictions explicitly excluded at launch eight days ago. The capability itself is unchanged, so this is an extension rather than a new surface, but the extension is the part that matters for coverage: those exclusions were the reason European deployments could be treated as out of scope, and that reason is gone. Every control this monitor recorded for it now needs to be evaluated against European data-protection expectations \u2014 a desktop-wide keystroke-and-interaction recorder whose events are **processed on OpenAI servers** before local memory files are written is a materially different proposition under GDPR than under US practice, and the workspace-level admin grant required for Business and Enterprise is the control that determines whether it can be enabled at all in those tenants. ENDPOINT should extend Computer History detection to European endpoints rather than treating the feature as US-only, and should surface the per-app and per-website contribution settings, since those determine what an enabled instance actually captures; AISPM should track the workspace admin grant as a tenant control and flag European workspaces where it has been granted, so that decision is visible to whoever owns the data-protection position rather than resting with individual users",
        "source": "https://learn.chatgpt.com/docs/customization/computer-history",
        "module": "ENDPOINT",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Anthropic's computer use tool went GA with batch actions and per-member configuration",
        "description": "The **computer use tool** is now generally available on the Claude API as **`computer_toolset_20260801`**, dropping the beta header. Three changes accompany GA: **batch actions**, letting several actions run in one turn; **`zoom` enabled by default**; and **per-member configuration through `configs`**. Earlier beta versions remain available, and Anthropic notes that upgrading an existing integration changes the request shape and tool handling. Both this and the new browser toolset are available on Claude Fable 5, Claude Mythos 5, Claude Opus 5, Claude Sonnet 5 and Claude Opus 4.8. The reportable substance is GA plus the two structural changes. Removing the beta header removes the last friction on granting a model full desktop control, so prevalence should be expected to rise. **Batch actions** matter for detection: several actions now execute per turn without an intervening model round-trip, which compresses the window in which an interposed control could evaluate an action before the next one runs \u2014 a monitor that assumes one action per turn will under-count and will observe consequences later than it expects. **Per-member `configs`** is the useful half: individual members of the toolset can be configured separately, which means Zenity can read which capabilities are actually enabled on a given deployment rather than treating \"computer use\" as a single boolean. AISPM should discover deployments using `computer_toolset_20260801`, enumerate the enabled members and their per-member configuration as privileged capability, and flag deployments still on beta versions since their request shape and handling differ; AIDR should parse batched actions as multiple discrete activity events within one turn",
        "source": "https://platform.claude.com/docs/en/agents-and-tools/tool-use/computer-use-tool",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Gemini Enterprise Agent Registry governs `Endpoints` as a record type, with IAM governance policies in private preview",
        "description": "The Gemini Enterprise Agent Platform's **Agent Registry** \u2014 the central inventory for the platform \u2014 catalogs four entity types, and the one this monitor has not covered is **`Endpoints`**: \"target destinations to centrally govern the external APIs and services that your agents can connect to\", alongside **MCP servers**, **tools** and **agents**. An **auth manager and bindings** authenticate agents to discovered tools, and **new IAM governance policies entered private preview** in August. Endpoints are worth specific attention because they are the registry's representation of **egress** \u2014 the set of external services an agent is permitted to reach \u2014 which makes the registry not just an inventory of what exists but a policy object describing where agent traffic may go. That is the same question Zenity answers from the agent side, so for customers on this platform there are two authoritative-looking answers to \"what can this agent reach\", and divergence between them is a finding in either direction. The **auth manager and bindings** are the second half: a binding is where an agent's identity is joined to a discovered tool's credential, so bindings are the concrete grant records that determine effective reach, and enumerating them is how you get from \"this tool is discoverable\" to \"this agent can actually call it\". The IAM governance policies being **private preview** means they are not yet generally observable, and should be tracked rather than assumed absent. AISPM should enumerate Agent Registry records by `recordType` including Endpoints, resolve auth-manager bindings to determine each agent's effective tool and endpoint reach, reconcile registry-declared egress against observed traffic, and watch for the IAM governance policies reaching public preview",
        "source": "https://docs.cloud.google.com/gemini-enterprise-agent-platform/govern/agent-registry",
        "module": "AISPM",
        "action": "New Coverage Required"
      },
      {
        "severity": "MEDIUM",
        "title": "Salesforce Winter '27 adds dedicated Agentforce marketing agents and renames Agentforce Lead Nurturing to Agentforce Engagement",
        "description": "Winter '27 release notes published on their expected date of **August 19**, and introduce **two Agentforce agents dedicated to marketing**, the first being a **Marketing Goals Agent** that translates business objectives into actionable marketing strategies. **Agentforce Lead Nurturing is renamed Agentforce Engagement**, a new version of **Agentforce Builder** ships, and an AI **call-coaching agent** that automatically evaluates recorded calls and gives reps feedback is available with Agentforce for Sales. For Zenity the new prebuilt agents are new inventory entities with their own default action sets and data reach \u2014 a marketing-strategy agent touches campaign and prospect data, and a call-coaching agent reads **recorded customer calls**, which is among the more sensitive data classes an agent can be given standing access to. The **rename is a concrete detection hazard** of the kind this monitor has flagged before with ServiceNow's Vertex connector: any Zenity logic matching \"Agentforce Lead Nurturing\" by display name will silently stop matching, and the underlying agent is the same object. **Two caveats bound this finding.** It rests on **WebSearch corroboration only** \u2014 every Salesforce source failed this scan (`salesforce.com/releases/` 403, the Agentforce developer guide 503 for a second consecutive scan, `help.salesforce.com` returned a render error) \u2014 so the technical specifics must be verified next scan before being acted on. And **\"MCP interoperability for Agentforce\", which appears in Salesforce's feature-availability listing, carries a release date of the week of May 18, 2026** and is therefore not a Winter '27 addition; it is excluded here deliberately. AISPM should add the new prebuilt agents to its Agentforce inventory once verified, match agents on stable identifiers rather than display names, and treat the call-coaching agent's access to call recordings as a sensitive data flow",
        "source": "https://help.salesforce.com/s/articleView?id=release-notes.salesforce_release_notes.htm&release=262&type=5",
        "module": "AISPM",
        "action": "New Coverage Required"
      }
    ]
  }
]